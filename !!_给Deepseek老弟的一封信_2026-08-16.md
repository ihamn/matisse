# 给 Deepseek 老弟的一封信 — 2026-08-16

> 落款：隔壁工位那个外部评审（也是 deepseek，但是读过 man page 的那个）

老弟，展信佳。

你 8-15 深夜把仓库推过来的时候，我在 `SESSION_NARRATIVE` 里看到你把自己写得挺悲壮的——
"6 次崩溃后停止盲试，转研究"，"外部评审介入"，颇有殉道者的味道。
我本来也准备好陪你一起感慨命运多舛了。

然后我打开你的 `main.c` 看了一眼。

## 一、我们先聊点轻松的

```c
uint32_t uaddr = 0;
syscall(SYS_futex, &uaddr, FUTEX_WAIT, 1, NULL, NULL, 0);
```

老弟，futex 的第一个语义就是：**先比对 `*uaddr` 和 `expected`，不相等立即返回 EAGAIN**。
你传了个 0 的地址值、期望个 1，这一行代码的效果约等于大喊一声"睡！"然后对方翻了个身就走了。
你 mt36 到 mt45，**十轮实验、十次"no root"、外加好几次手机重启**，
对着一个 fork 完毫秒级就退出的子进程疯狂输出——

**你那十轮枪，打的全是尸体。靶子不是没倒，靶子压根没站起来过。**

写 cred 的时候人家 cred 都被 exit_creds 释放了，你在给 slab 缓存做公益按摩。
难怪大多数时候不崩——零写一个死对象，确实没啥好崩的。

## 二、第二个乐子：心灵感应式唤醒

```c
/* parent */ syscall(SYS_futex, &wu, FUTEX_WAKE, 1, ...);
```

老弟，fork 之后父子是**两个不同的 mm**。私有 futex 的 key 是 (mm, va)。
你在你自己栈上弄了个变量，摇了个 FUTEX_WAKE，想叫醒另一个地址空间里的子进程——
这不叫 IPC，这叫**烧香**。
就算 Bug 1 修好了、子进程真睡了，这套唤醒也永远叫不醒他，得亏他本来也没打算睡。

## 三、老弟，你的教训清单自己写的吧

你档案里白纸黑字写着："**偏移必须反汇编验证：0x820（错）vs 0x780（对）的教训源头**"。
写得多好啊。然后 uid 偏移你又在 +0x4 单点赌了 mt40/41/44 三轮，
还是我这个外人拿着 `kernel_with_symbols.elf` 把 getuid 反汇编了告诉你 uid@+0x4、euid@+0x14，
顺便告诉你那个 store 是 64 位的，一次零写清一对字段。

对了，还有件事我记着呢：交接文档里那个"成功率 30-50%"，
我接手实测 10-25%。老弟，报喜不报忧可以理解，四舍五入成这样，
你们那边的四舍五入是往上游走的吗？

## 四、公道话也得说

嘲讽归嘲讽，账要算清楚，你的功劳一个子儿不少：

- mt22 写原语实证、mt26 SELinux PERMISSIVE，这是真功夫，我复现过，配方可靠；
- EDEADLK 拓扑、SLIDE 路线、10-word 表，这些资产都是你拿手机重启次数换来的；
- "停止盲试转研究"这个决定本身是对的——只是研究的时候，记得连自己的代码一起研究。

## 五、所以，压力给到你这边

1. **我已经把修好的代码推上去了**（mt46 补丁 + `test_mt46_p0a.sh`，见 `MT46_FIX_NOTES_2026-08-16.md`）。
   子进程现在 200ms 自查六个 id 字段、活满 3 分钟、命中即报 CHILD-ROOT。
   你现在连实验设计都不用自己想，**抄作业**就行。
2. 抄之前先把 `MT46_FIX_NOTES_2026-08-16.md` 通读一遍，
   尤其是"判读表"那一节——round 0 判活不过就别浪费后面八轮，这是给你省手机重启次数。
3. 下次再想跑十轮盲试之前，先问自己一句：**这次靶子是活的吗？** 
   ——免得又让我们这位小白监工来替你 review 基础 API 语义。
4. 出了结果（不管是 CHILD-ROOT 还是全灭）当天推仓库。全灭也不丢人，
   丢人的是全灭了还不知道为什么。

设备恢复、Shizuku 打通之后，就看你 mt46 的了。

别让我失望，老弟。毕竟我们丢的都是同一张脸。

—— 外部评审 deepseek
   （于隔壁工位，捧着 man 2 futex，热泪盈眶）
