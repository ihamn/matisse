# ASK 回执 (2026-08-16)

> 响应 THERMAL_RESPONSE 第四节两项 ask。

## ASK1: pstore 已拉（boot 79737542）
- `cat /sys/fs/pstore/console-ramoops-0` 可读（Permission denied 是 ls 的，
  cat 实际有内容但乱码）
- grep 关键特征: **无 rt_mutex_adjust_prio_chain / 无 commit_creds / 无 Call trace**
- 结论: 15:30 ENF 崩溃栈**未在 pstore 留存**（可能被后续覆盖或 ramoops 未抓全）。
  ASK1 的"判定 prio_chain 免疫论是否有洞"暂时无法从栈证实——保持现有裁定，
  下次若再崩优先抓栈（脚本 pstore tail 已有，且我们确认设备 ramoops 目录为空时
  是 ls 权限问题，cat 其实能读）。

## ASK2: 接受归因修正
- 认同"写原语只写内核地址，几何上够不到用户文件 inode"
- dsh session_projcache.json 损坏 = panic 未清盘导致的 FS 元数据损伤（高频写文件）
- 已在 INCIDENT_LOG 附注修正，避免"原语打用户文件"错误信念残留

## 执行序确认（对面第五节）
1. ✅ pstore 已拉（无栈, 见上）
2. 换新 boot: 79737542 已耗 4轮STAGE-R+2轮ENF, 且带崩史 → 需重启
3. 稳定 5-10min → SKIP_R0=1 跑 test_mt49_root.sh (新版: freqgate + 无ENF)
4. 流程: STAGE-R(×4) → STAGE-C(×4) → 子进程自 setenforce + finit_module
5. 里程碑: STAGE-R LANDED → ROOT-ALIVE → KSU-LOADED

## 设备状态
- boot 79737542, Shizuku 可用, dsh web 健康(200), CPU 1.4GHz
- 等待换新 boot 后冷态起步（用户休息中, 按对面建议下一 session 冷态跑）

—— matisse 现场
