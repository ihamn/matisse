# 连续黑屏卡死裁定 + 战略绕行方案（评审，2026-08-16 第六班）

> 四问全答，外加一个你们没问但最值钱的问题：**A1 这条 hang 高发区，
> 可能根本不需要跑。** 见 §五。dsh 污染修正（用户抓的幸存者偏差）收下，
> 见 §六。

---

## 一、Q4 先答：长按重启说明什么

`reboot_longkey` = 电源键长按 ≥7s 触发的 **PMIC 硬件级复位**——它永远有效，
与内核死没死无关，所以**长按本身不携带"冻结深度"信息**。真正的分类器是
**长按之前 adb/Shizuku 还应不应**：

- adb 死 + UI 死 = 内核级 wedge（调度器/锁级）
- adb 活 + UI 死 = 用户态关键服务 wedge（system_server 挂 D 态）

**请用户回忆两次卡死时有没有试过 adb / 无线调试有没有掉线**，这个答案
直接二分机制。

## 二、Q1：机制——未定，两个活假设，都吻合"零打印卡死"

| | 假设 A：负载活锁 | 假设 B：PI 链关键服务 wedge |
|---|---|---|
| 机制 | fork/spray 耗尽内存 → 直接回收风暴饿死一切 | 毒树/PI 链把 system_server 关键线程挂进 D 态 |
| 为何零 printk | 调度器活着，看门狗视角一切正常 | 用户态 wedge，内核根本没崩 |
| 为何 dsh 污染随机 | 强制复位时 FS 脏态随机 | 同左 |
| 证据指纹 | pstore 尾部应有 OOM/reclaim 刷屏 | pstore 尾部安静或停在无关日志 |

**区分两者的钥匙就是我已经要了三次、还没拿到的东西**（见 §四）。

时间模式我注意到一个：**三次 hang 全部发生在"新 boot + ARC 自动序列"里**
（hang#1 还是 ARC round2 距离上轮击杀仅 2 秒——违反我上一份裁定的 60s
纪律，说明 ARC 包装脚本没实现护栏）。今天所有**手动**轮（A1 win、B1）
一次没 hang。ARC 自动化是和 mt56 同时引入的新变量——见 §三。

## 三、Q2：mt55 A/B？—— 不做，混杂变量已找到

mt56 维持机械排除，且把事实说得更精确（我核了源码）：

1. mt56 的假 cred 块在 `PAGE_PAYLOAD_SLIDE` 分支内（util.c:689 起），
   **ENF 轮确实执行这些写入**——但写入的是惰性字节（无 PTR win 就没有
   读者），且最远只写到 0x3898（mt35 写到 0x38a0），足迹严格更小。
2. **表面相关性（mt56 后 3 hang）的混杂变量 = ARC 包装脚本**：它和
   mt56 同批上线、它不守 60s 间隔、它不等 boot 沉降、它在新 boot 上
   自动连环。手动轮 0 hang 的对照比任何 A/B 都干净。
3. 专门 A/B 要烧 boot + hang 风险，换不来机制信息。**先做 §四 的
   零风险取证**；若证据拿不到，才允许一轮**手动、沉降后、带内存门**
   的 mt55 A1 作对照。

## 四、取证清单（下次开跑之前，全部零风险）

1. **pstore 原始尾部 50 行**（第三次要求）：别再 grep "Call trace"，
   261KB 的最后 50 行直接推。打印停在哪 = §二 表格的判别器。
2. **卡死轮次的 .out 文件**：`/data/local/tmp/A1_r2.out` / ARC2 的
   输出文件**在重启后仍活着**（文件系统是持久的）——最后一行 flush
   到哪个阶段（喷页中/触发 burst 中/consumer 中）就是冻结时刻。
3. **ARC 脚本源码推仓库** + 它的时间戳：ARC2 启动时距 boot 完成多久？
   轮间隔多少？——验证"新 boot 未沉降 + 自动连环"假设。
4. 用户对 §一 adb 问题的回忆。

## 五、★战略绕行：R 不需要 Permissive（未被隔离验证的继承假设）

现在 hang 全在 A1（翻 Permissive）阶段。问题是：**为什么还要翻
Permissive？** 翻盘是 ENF 时代的历史惯性，R 轮的全部机器里对 SELinux
敏感的依赖只有一处——perf 泄漏。而我刚核了源码：

```c
main.c:754  if (!task) {
main.c:755    pr_error("mt28c: perf task leak failed\n");
main.c:756    return 1;      ← 触发 burst 之前干净退出
```

**perf 若被 Enforcing 拒绝，R 会在开枪前自己 abort（零风险、零触发）。**
也就是说：Enforcing 直打 R 是自探测的——不需要单独的探针轮。

**下一 boot 的首选动作（替代 A1→R→C）**：

```
沉降 10min → 直接 R（mt56, PTR_RIGHT=auto, SKIP_WARMUP=1,
            WAIT_SECONDS=200, Enforcing 原样）
```

判读：
- RUNLOG 出现 `mt28c: perf task leak failed` → perf 确实需要
  Permissive，A1 流程恢复（带 §四 后的新纪律），我们零成本学到边界
- 泄漏成功 + PTR win + mt56 假 cred → **不翻盘直接拿 root**（假 cred
  是满帽 + kernel SID，win 后 setenforce 0 都可以做），A1 的 hang 区
  整条绕过；且从此不再怕重启丢 Permissive（每 boot 直接打）

风险声明照旧：PTR win 后的存活赌的是 mt56 修复 + mt53/54 防护，
与 Permissive 无关（R 上次的 selinux hook 崩溃在 Permissive 下照样
发生——hook 两种模式都跑）。**这条路没有引入任何新风险，只是拆掉了
一个从未验证过的前置依赖。**

## 六、dsh 污染修正：收

用户抓的幸存者偏差是对的，最终结论（污染与关盘脏态相关、与事故类型
无关）与我的 ASK2 归因一致，此账翻页。一个技术性小纠：隔离目录的
mtime = **下次 boot 时 dsh 创建它的时间**，不是污染发生时刻——
"20:37:59 精确吻合"是过读，别把它当时间锚用（碰巧接近而已）。

## 七、恢复条件（给现场）

1. 先交 §四 1-3（不占轮次，不碰设备运行态）
2. 用户答 §一 的 adb 问题
3. 然后二选一：**首选 §五 R 直打**；若泄漏被拒 → A1 恢复但必须：
   手动逐轮、boot 沉降 ≥10min、轮间隔 ≥120s、每轮前 MemAvailable
   门 + `ps` 查残留、轮内每 10s 采一次 meminfo 追加进 runlog
4. **fork/spray 数量一个都不许动**——配方刚被实验定罪为 win 因素，
   动它 = 重演 0/9
5. 本 boot hang 预算 2；再 hang 就收工交证，今晚不再开新 boot
   （用户已经长按两次了，疲劳本身是风险）

—— 评审 · 核心判断：mt56 无罪维持，真嫌疑 = ARC 自动化 × 新 boot
未沉降 × 负载；最优解可能不是修 A1 而是跳过 A1
