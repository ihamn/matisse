# crash#3 机制裁定 — +0x14c0 完整定罪 + ENF/PTR 存活分裂的解释框架（外部评审，2026-08-16）

> 回复 `PSTORE_CRASH3` + `STATUS_EVIDENCE` 的裁定请求。
> **一句话：BUG 的精确指令级机制今晚拿到了（+0x14c0 三条指令）；写与崩在
> 同一次 walk 内 —— mt51 原理上防不住，任何用户态防护都防不住；同时我
> 修正自己两个错误；ENF 存活/PTR 必崩的分裂有了领跑解释（喷页转向假说），
> 它直接指向路线修复方向。**

---

## 一、先修正我自己的两处（诚实清单又长一行）

1. **"换成 Case-1" 的念头是废话**：读 `main.c:817-822`，PTR 模式的几何
   **本来就是 Case-1**（`left_env="0"`，R: pc=task+0x770→[0x778]，C:
   pc=task+0x778→[0x780]）。我在脑内推导的"新路线"就是已经在跑的代码。
   好在发布前读了代码，没闹笑话。
2. **mt51 对本类崩溃无效，且原理上不可能有效**：BUG 发生在承载写入的
   **同一次系统调用内部**（erase 之后、返回用户态之前的 walk 步骤）。
   没有任何用户态检查点能插进"写落地"与"断言触发"之间。mt51 的发间
   检测/fsync 仍是好卫生（对别的风险面），但它救不了这个。

## 二、BUG 的精确机制（+0x14c0，今晚新反汇编）

```
rt_mutex_adjust_prio_chain:
+0x14c0: ldr  x8, [x27, #0x10]     ← x27=当前walk的lock; [lock+0x10]=waiters.rb_leftmost
+0x14c4: cbz  x8, ...               ← 树空则跳过
+0x14c8: ldr  x9, [x8, #0x38]      ← leftmost_waiter->lock   (waiter+0x38 = w7!)
+0x14cc: cmp  x9, x27               ← 与当前 lock 比较
+0x14d0: b.ne → brk#0x800 簇        ← ★ 不等 = BUG (rtmutex_common.h:60)
```

**问题本质**：这个完整性检查要求"树的 leftmost waiter 自称属于这棵树"。
我们的 w7 盖的是 `fake_lock`（喷页/静态锁），而 walk 的 x27 是 f_pi_target
的**真内核 rt_mutex 对象** —— 它的地址在用户态不可知。所以：

> **经由 prio_chain 的树手术写入，这个检查结构性不可满足。**
> 不是校准误差，不是时序，是墙。w7 永远 ≠ 真锁地址。

（细化了我此前 +0x1788 的裁定：同一断言簇，现在有了准确的触发指令和
比较语义。）

## 三、由此重写今天的账本

| 事件 | 旧解释（错/不准） | 新解释 |
|------|------------------|--------|
| crash#2 | 同进程二触/进程内 ALT | **PTR 几何第一次赢**：写落地 → 同一 walk 下一步撞 +0x14c0 |
| crash#3 | "污染后 sched_setattr" | **PTR 几何第二次赢**：同机制，与二触无关 |
| 下午 6 连灭 | 环境/降频 | 触发未赢（miss）→ 无写无崩 —— 与环境可能全无因果 |
| mt49 一进程一写 | "结构性免疫" | 免疫论对象错误：崩不在 attempt 间，在 walk 内 |
| 上午 ENF 4 连成 | "零写路线安全" | 见 §四 —— 安全的真正原因可能不是"零" |

回答现场的三个问题：
1. **RETRY=1 免疫破没破** —— 问题本身失效了：崩不在"第二次触发"，
   在承载写入的那次 walk 内部。attempt 数量无关。
2. **mt50/prepare 有没有新问题** —— 没有。mt50 的改动在 finit_module
   阶梯，本轮根本没执行到。prepare 与下午版本同源，下午几十次无一崩。
3. **mt51 覆盖吗** —— 不覆盖，也不可能覆盖（§一之 2）。

## 四、ENF 存活 / PTR 必崩的分裂 —— 领跑假说：喷页转向

对齐两边全部参数（`ELF_ANALYSIS` 表 + slide.c:108-135）：

| 参数 | ENF（4/4 存活） | PTR（2/2 panic） |
|------|----------------|------------------|
| w2 left | 0 | 0（相同） |
| w0 PC | selinux_state dmap 区 | task+0x770（task 页） |
| w1 RIGHT | **喷页地址 0x...28a7700（内容全可控）** | **init_cred dmap（内容=内核的，固定）** |
| STORE(a) | [selinux+8]=喷页指针（低字节 00 → enforcing=0） | [task+0x778]=init_cred |
| STORE(b) | **[喷页]=pc（写进自己的页，无害且内容继续可控）** | [init_cred]=pc（污染 usage/uid） |
| PSELECT_CRED | 无 | 有（fork/perf 泄露机制在跑） |

**假说**：erase 之后内核会做 leftmost 重算（`rb_next(旧节点)`）。旧节点的
右子 = w1 = RIGHT：

- ENF：rb_next 走进**我们的喷页** —— 页内容我们全控 → 喷页里的"节点字段"
  把后续 walk 引向安全出口（这就是 v37/SLIDE 设计里喷页真正的角色：
  **不只是写落点，是转向器**）。4/4 存活。
- PTR：rb_next 走进 **init_cred** —— 内容是内核的 uid/caps 字段当指针用，
  完全不可控 → 下一步就是 +0x14c0 拿垃圾比真锁 → 必崩。2/2 panic。

这个假说**同时解释全部分裂现象**，且给了一个可证伪预言：ENF 形状的
喷页内容如果在 +0x38 处不是精心值也该崩（历史上喷页是零页 + 布局，
4/4 没崩 —— 说明零页恰好满足出口条件，值得反汇编确认确切要求）。

诚实标注：**假说，未证明**。证明路径在 §五。

## 五、下一步（分工明确）

**我的下一动作（今晚/明早，我的沙箱）**：反汇编 dequeue→leftmost 重算→
walk 出口的完整路径（rt_mutex_dequeue 的 rb_next 逻辑 + prio_chain 出口
条件），把"喷页需要在哪些偏移放什么值才能走安全出口"写成精确 spec。
这是 §四 假说的证明，也是路线修复的地基。

**现场的下一动作（可选，一次 boot 内做完，零新风险面）**：
- **ENF+PSELECT_CRED 探针**：ENF 原配方（RIGHT=默认喷页、PC=selinux）
  + `PSELECT_CRED=1`。若崩 → 嫌疑转到 PSELECT_CRED 机制；若存活 →
  排除该变量，假说加强。这是矩阵里唯一还没单独隔离的变量。
- **维持纪律**：不再跑 PTR 形状（2/2 结构性 panic，§二的墙）。
- 每轮照旧 pstore。

**路线修复方向预告（等我的 spec）**：若喷页转向假说成立，cred 写入的
正确形态是让 w1 指向**喷页上的假 cred**（内容全控：uid=0、caps 满帽、
usage 巨大），而非 init_cred —— 代价是假 cred 的 `security` 指针问题
（avc 会解引用），需要 spec 阶段一并解。两阶段 STAGE-R/C 机制全部复用。

## 六、风险与状态盘点（收盘）

- 路线状态：**主几何（PTR→init_cred）判死**；替代通道（喷页转向）存在
  且有 4/4 实证背书，但需要我的反汇编 spec 落地。
- 项目最大资产未损失：触发链稳定性（上午 4 成 + 全天几十次 miss 无崩）、
  perf 泄露、两阶段脚本、状态文件、gate、finit_module 梯子 —— 全部复用。
- 今日净结论：我们不是"离 root 更远"了 —— 是终于知道墙在哪，以及
  墙旁边那条 4 次走通过的路。

—— 外部评审（今晚第二份认错报告，但这次带着墙的坐标）
