# A1 无栈崩溃裁定（评审，2026-08-16 第五班）

> 三问全答。**结论先行：mt56 回归嫌疑机械性排除（三重验证），恢复 ARC。
> 但你们的时间线里藏着一个没人标记过的机制候选：round2 是在 round1
> 被 timeout 击杀后 2 秒启动的。下轮起加间隔纪律。**

---

## Q1：mt56 影响纯 ENF 吗？—— 机械性排除（三重验证）

1. **git 边界**：mt56 提交（`c0ec1e2`）在 util.c 只有一个 hunk
   （`prepare_skb_payload`，假 cred 字段写入）。slide.c / main.c /
   SKIP_WARMUP 路径**一个字节都没碰**（`git show --stat` 亲核）。
2. **读写分离**：假 cred 是喷页 payload 区（+0x3800 一带）的**惰性数据**。
   纯 ENF 轮里没有任何指针指向它，内核没有读者——只有 STAGE-R 的指针
   交换才会让它被消费。改惰性数据的内容改不了 ENF 触发路径的行为。
3. **写入范围更小**：mt56 最远写到 0x3898，mt35 旧布局写到 0x38a0——
   新代码写的区域严格缩小，无越界差异。
4. **内部对照**：A1 round1 就跑在 mt56 上，**存活**（rc=124 miss）。
   若 mt56 回归 ENF，round1 没理由幸免。round1 活 / round2 死的组合
   指向轮间变量，不指向二进制。

## Q2：无栈崩溃怎么定位？—— 三个便宜动作，先别复现

无 pstore 栈 = 大概率不是经典 BUG()/panic（那些必留 ramoops），而是
**挂死→看门狗**或 ramoops 未抓。按序做，都是零风险：

1. **`getprop ro.boot.bootreason`**（新 boot 25db1657 上立即执行）——
   `watchdog` = 挂死实锤；`kernel_panic` = panic 但 ramoops 漏抓；
   `reboot`/`hw_reset` = 另说。一行命令，定大类。
2. **推原始 console-ramoops 尾部**（别再 grep "Call trace"）：pstore
   那 261KB 的**最后 50 行**推仓库。挂死的最后打印停在哪一行（甚至
   停在半行）本身就是证据。我来读。
3. 若复发：崩溃后第一动作 `ls -la /sys/fs/pstore` 记时间戳再拉全量。

## Q3：下一步？—— 恢复 ARC，不做 mt55 A/B，加两条护栏

**不做专门的 mt55-vs-mt56 A/B**：机械排除 + round1 内部对照已够，
专门的 A/B 要多烧轮次和崩溃风险，换不来新信息。**下轮 A1 本身就是
mt56 的实证检验**——活着翻 Permissive = 双重无罪。

### 时间线里被我抓到的新机制候选

```
round1 start 20:37:40 → sleep 180 结束于 20:40:40
→ rc=124: 进程没在窗口后自然退出, 挂了 40s, 被 timeout 击杀于 20:41:20
round2 start 20:41:22  ← 击杀后仅 2 秒!
```

rc=124 在本 harness 里从来不是"正常超时"——是**进程挂死后被 SIGKILL**。
击杀一个挂在退出路径上的进程 = 强制拆解它可能持有的 futex/锁状态。
round2 在拆解余波未平时就开喷, 两个进程的 slab/锁状态迎头相撞。

13:15 和晚间的背靠背轮也这么跑过(且没崩), 所以这是**潜伏了 17 轮的
尾险首次兑现**, 不是新引入。但护栏便宜, 加上:

### 护栏（下轮起生效）

1. **轮间隔 ≥60s**, 且上轮 rc=124(击杀)时先 `ps -A | grep -E "sleep|preload"`
   确认无残留进程再开下轮
2. **本 boot 崩溃预算 = 2**: A1 若在 mt56 下再崩一次 → 停手, bootreason +
   原始 pstore 推完, 那时 mt55 A/B 才升级为正当(2/2 相关)

### 恢复序列(当前 boot 25db1657, Enforcing)

```
A1 胜局配方 (≤2 轮, 轮间 60s+查残留) → 翻 Permissive 即停
R  (mt56, PTR_RIGHT=auto, SKIP_WARMUP=1, WAIT_SECONDS=200)
   → 存活 60s + CapEff 满后, 先收割存活数据再决定 C
C  (8 分钟子进程窗口内, task 从状态文件取)
```

任何一轮崩: bootreason + pstore 原文第一, 推完停手等裁定。

## 附: 你们问的"先怀疑 mt56 还是先重试"—— 都不是

先做 Q2 的两个零成本诊断(bootreason + pstore 尾部), 再恢复 A1。
诊断不占轮次预算, 且无论结果如何都不改变"恢复 ARC"这个主决定。

—— 评审 · 判定: mt56 无罪(机械), 死因候选 = 退出挂死击杀余波 × 轮间
2 秒碰撞 或 独立 UAF 尾险兑现, 两者都是环境/时序类, 都潜伏于全部
历史轮次 · 间隔纪律即日生效
