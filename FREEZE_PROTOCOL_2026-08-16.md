# 冻结响应协议 + 用户观察补充（评审，2026-08-16 第六班附）

> 用户补充了卡死现场观察：**黑屏一片、无法开机/唤醒，长按恢复**。
> 用户没有试 adb（黑屏时想不到，正常）。此协议取代"直接长按"。

## 一、冻结 ≠ panic（给现场的定性别再混用）

- **panic（crash#1/2/3）**：内核主动报错 → ramoops 留栈 → 主动重启。有遗书。
- **冻结（hang#1/2/3）**：没人出错，全员停进——调度器活着、看门狗视角
  "一切正常"、**零打印**。没有遗书，这不是 pstore 抓漏。
- 用户问"为什么没解释"：不是你藏着，是这类事故**机制上不产生现场**。
  证据要从别处拿（§三）。

## 二、下次冻结的操作序列（用户侧，每步 ≤30s）

1. **短按电源 2 次，等 60s**——"饿死"型可能随负载退潮自己复活
2. **电脑 `adb devices` 探一下**——★adb 在线 = 内核活、用户态挂
   （PI 链 wedge 假设）；adb 死 = 内核级 wedge（活锁假设）。一行输出
   直接二分机制假设，是目前最便宜的判别器★
3. 前两步无果 → 长按电源 ≥7s（PMIC 硬件复位，永远有效）
4. 复活/重启后第一件事：`ls -la /sys/fs/pstore` 记时间戳，再拉全量

## 三、欠账不变（第三次催）

1. pstore **原始尾部 50 行**（别 grep，直接推）
2. 卡死轮的 .out 文件（`/data/local/tmp` 里，重启后仍在）——
   最后 flush 到哪一行 = 冻结时刻
3. ARC 脚本源码 + 各轮时间戳（验证"新 boot 未沉降 + 2s 轮距"）

## 四、下一轮动作不变

**首选仍是 Enforcing 直打 R**（mt56 + PTR_RIGHT=auto + SKIP_WARMUP=1
+ WAIT_SECONDS=200，沉降 10min 后）。A1 冻结区整体绕开——用户不该
再遇到黑屏。A1 只在 R 的 perf 泄漏被 Enforcing 拒绝（开枪前自动 abort，
零风险）后才恢复，且必须手动逐轮 + 新纪律。

—— 评审 · 冻结本身零数据不是坏消息：它排除了一整类"主动崩溃"机制，
把假设空间压到两个，而 §二-2 的一行 adb 输出就能再砍掉一个
