# 项目交接文档 — matisse (Redmi K50 Pro) KernelSU 提权 (2026-08-15 晚)

## 目标
锁 bootloader 的 Redmi K50 Pro (matisse, MTK Dimensity 9000) 上装 KernelSU:
CVE-2026-43499 (rtmutex UAF) 临时 root → insmod android12-5.10_kernelsu.ko。

## 设备
- HyperOS 2.0.6.0.ULKCNXM, kernel 5.10.209-android12-9 (GKI, KASLR 关闭 slide=0)
- 12GB RAM, 39-bit VA, PHYS_OFFSET=0x40000000, P0_PAGE_OFFSET=0xffffff8000000000
- KIMAGE_TEXT_BASE=0xffffffc008000000; 关键地址: enforcing=0xffffff8002a41b98,
  boot_id=0xffffff80028a77d0, init_cred=0xffffff80027b0ae0, INIT_TASK=0xffffff800279bec0
- shell 通过 rish (Shizuku, uid=2000, 无 seccomp), perf_event_paranoid=-1

## 已验证的写原语 (确定性!)
- 触发 (v37 拓扑 + pselect overlay): FCRQ→EDEADLK + FWRQ→timeout → 栈释放 →
  pselect fd_set 覆写 freed rt_waiter 槽 (word0-9) → futex_lock_pi 驱动 walk → rb_erase 写
- **主树写 (tree_left=0, Case-1): 写值 = tree_pc 派生 (byte0=0), 落点 = tree_pc+8**。
  mt26: tree_pc=enforcing-8 → 写 byte0=0 → Permissive (成功过, round4)
- 10-word waiter 布局: word0=tree_pc(+0), word1=tree_right(+8), word2=tree_left(+0x10),
  word3-5=pi_tree(0), word6=task=INIT_TASK(+0x30), word7=lock=fake_lock(+0x38),
  word8=prio=130(+0x40), word9=0(+0x48)
- 5.10 rbtree: RB_RED=0, RB_BLACK=1 (bit0=1=黑). rb_erase 的 Case-2 (tree_left≠0):
  rb_set_parent(tmp,successor) → *(tree_left)=tree_right = **任意值写** (源码+反汇编确认)
  但 Case-2 在真机不稳定 (见崩溃点)

## 当前状态 (2026-08-15 晚)
- **SELinux: Enforcing** (mt26 的 Permissive 是内存态, 重启即失效)
- 手机刚重启 (干净), boot_id=b14bd577
- /data/local/tmp/preload.so = preload_mt25.so (sha256 ed611d13, 早晨成功的版本!)
- 测试循环: run_mt26e.sh (1轮 = ks ~3min + 触发 10s, timeout 300)
- 结果: 每轮 ~30-50% 写成功 (不崩就是多试), 早晨 mt26 round4 成功 → Permissive

## 崩溃点 (pstore 已确认)
1. rtmutex_common.h:60 = rt_mutex_top_waiter 的 BUG_ON(w->lock != lock):
   悬垂槽是 waiters 树 leftmost 时 lock 字段≠真锁 → panic (mt28i)
2. 写 init_cred.usage (活 refcount) → 竞争崩溃 (mt28h)
3. rt_mutex_adjust_prio_chain+0x188 数据访问错误 (96000004/5) (mt28f/d 静态锁/mt26d)
4. 野写可损坏 sidtab → unknown SID → avc 全失败 → 系统崩 (息屏后自发写触发)

## 已验证教训 (必读!)
- **写完不能留悬垂链过夜**: 自发写在系统任何 futex 事件时触发, 写已释放内存 → 损坏 sidtab/文件
- **/sdcard FUSE 会把文件写成全零** (构建产物/拷贝都会中招!) → 构建到 $HOME (ext4) 再传
- **重启会丢 /data/local/tmp 未刷盘写入** → 传完 sync + 验证 magic
- **perf_find_task 必须在 ks 之后运行** (之前会破坏 memfd open → Permission denied)
- **进程内重试毒化**: [5] trylock 写 wait_lock → 重试必须独立进程
- **LOCK_OFF+0x80 是 ks 雷区** (mm_struct 字段, 别放假数据)
- Shizuku 每次重启后要手动重启; rish 间歇性超时 (重试即可)

## 交接点: 下一步
1. 用 preload_mt25.so + mt26 配置 (run_mt26e.sh) 循环跑 → 拿 Permissive (安全网)
2. Permissive 后 pstore/dmesg 可读 → 崩溃可精确诊断
3. 再攻 Case-2 任意值写 (假 cred @SLIDE页0x3800, 或修 tree_left 拓扑)
4. root 后: setenforce 0 → 装 KernelSU

## 文件位置
- 项目根: /storage/emulated/0/Documents/matisse_backup_essentials/
- 主归档: AI_ARCHIVE_2026-08-14_kallsyms_verified.md (mt28 进展见尾部)
- 源码: _research/mt11_v30/ (Makefile: make OUTDIR=$HOME/x CC="clang --target=aarch64-linux-android35" PROJECT=matisse-OS2.0.6.0.ULKCNXM)
- 成功二进制: preload_mt25.so (ed611d13) — 早晨 mt25/mt26 写入成功的那版
- 内核符号: _research/_extract/kallsyms.txt (142102 符号, slide=0 直接可用)
- 内核源码: _research/android_kernel_xiaomi_matisse-main/ (rbtree.c/rtmutex.c 已提取)
