# 项目交接文档 — matisse (Redmi K50 Pro) 提权 (2026-09-02, mt72 / E4 pi-tree 就绪)

> 承接 `logs/HANDOFF_20260830.md`（两阶段换指针结构）与
> `CHECKPOINT_C_stage_paradox_20260901.md`（C 悖论指令级证明，本文的完整理论依据）。

## 目标与设备
- 锁 BL 的 Redmi K50 Pro (matisse, MTK Dimensity 9000), HyperOS 2.0.6.0.ULKCNXM,
  kernel 5.10.209-android12-9, KASLR 关闭 (slide=0)。
- CVE-2026-43499 (rtmutex UAF) 临时 root → KernelSU。
- 关键地址: init_cred=0xffffff80027b0ae0, INIT_TASK=0xffffff800279bec0,
  enforcing=0xffffff8002a41b98。shell 经 rish (Shizuku, uid=2000)。

## 漏洞利用结构（两阶段换指针）
- **R 阶段**: 主树 rb_erase STORE 写 `[task+0x778] real_cred = init_cred` → 子进程 CapEff 满帽。
  **已 7/7 稳定落地**（`logs_raw/20260901_c_geom_decisions/`）。
- **C 阶段**: 写 `[task+0x780] cred = init_cred`。**实测 0/N 全灭**（fork/external × 3 种进窗全组合）。
- 两写都落地 → `real==cred==init_cred` → 过 commit_creds 的 `BUG_ON` →
  子进程 AND-gate (CapEff 满 && euid==0) → setresuid(0) → root。

## 核心谜题：C 悖论（已指令级证明）
逐指令反汇编 `rb_erase` CASE_A（`ref/rb_erase.asm`）证明 C 几何**必然**写 0x780：
- C(pc=0x778) 检查 `*(task+0x788)`；0x788 是未知 8 字节字段（非 comm——comm 在 0x790，
  `scripts/dump_task_fields.py` init_task dump 实证），初值 0 且全内核无访问者
  （`scripts/find_offset_access.py` 扫 .kernel 全符号 0 hits）→ 比较必 ne → STORE 必落 0x780。
- **指令级应成功 vs 实测 0/N = 结构性矛盾**。两类假设：H2（erase 的 store 未执行，
  触发链在上游）vs H3（写落了但事后被中和/回滚）。
- 附带取证：Cfork 子进程 ~10s 内死亡无心跳（`logs_raw/20260901_mt71_cfork/`）。

## 本次交付：mt72 = mt71 + E4 pi-tree 备用几何
构建详情见 `bin/mt72/BUILD_INFO.txt`（preload.so 188632B,
SHA256 7f74af2dd6766c5b794e9a8efc789c7f5a72a355495d34b46fae1182c3bcf78d）。

### E4 原理（绕开主树 C 几何）
fake waiter 的 **pi_tree_entry（word3-5）走同一个 `rb_erase`**，但用"继承色"路径：
- `PI_PC(word3)=写值`(init_cred 别名), `PI_RIGHT(word4)=task+0x780`, `PI_LEFT(word5)=0`
- → child=word4≠0 → 无 rebalance → 检查 `*(word3+0x10)==node?` ne（init_cred+0x10=euid/egid=0）
- → STORE(a) `*(word3+8)=word4`（init_cred gid/suid 区污染，无害）
- → **STORE(b) `*(word4+0)=word3` = `[task+0x780]=init_cred` ★cred 写★**
- 主树无害化：TREE_PC=fake_lock 喷页零区、RIGHT=LEFT=0 → CASE_A child=0 只写零区自身。

### main.c 三处改动（沙盒重置后已全部重建并逐条验证）
1. E4 块：`PSELECT_PTR_PI=1` 启用（fork 隔离 / external 补写两用法；
   可选 `PSELECT_PTR_VALUE` / `PSELECT_PTR_TARGET_OFF` 覆盖默认）；无喷页则安全中止 attempt。
2. **bug 修复**：attempt 循环原本无条件把 `PSELECT_PI_PC/RIGHT/LEFT` 清零 →
   E4 的 PI 词被抹掉、pi erase 退化全零 root 路径、写静默失效。已加
   `if (!getenv("PSELECT_PTR_PI"))` 守卫。
3. **bug 修复**：PI 轮显式 `setenv("PSELECT_PTR_STAGE","C")`（mt51 中止信号=euid==0）。
   否则 external E4 轮在 R 已把 CapEff 打满后，R 语义（CapEff 满即停）会首发后误停 → E4 静默变单发。

## 现场实验（环境恢复后按序执行，均已含 E2 取证：1s sampler + status 历史 + 最终 child status 采集）

前置：Shizuku 运行、屏幕常亮插电、`bash termux/deploy_mt72.sh` 出 DEPLOY_OK、
consumer 未被饿窗（连续 0 发时先等调度恢复，勿硬跑——见 PROGRESS.md 2026-09-01 崩溃教训）。

| 序 | 命令 | 判定 |
|---|---|---|
| 1 | `bash termux/run_E4a_test.sh` | 成功：child status 出现 `euid=0` 且 `CapEff=0000000000000000`（半程态，STRICT AND-gate 结构性不触发，安全）。失败：euid=2000 不变 → pi 几何也被挡 → H2 上游，转 E1 取证。 |
| 2 | `bash termux/run_E4b_chain.sh` | R 轮落地（CapEff 满）自动进 E4 轮 external 补写。成功：`root_alive.txt` / ROOT-SEEN。R 未落地则脚本自动跳过 E4 轮，稍后重跑。 |
| 3 | `bash termux/run_E1_gradient.sh` | PC_OFF 三梯度 {0x770, 0x788, 0x778}。判别：0x770 落地 && 0x788 轮 comm 被污染 → 写确实落在 task+0x78X，仅 0x780 被事后中和（H3，E4 是正解）；0x770 落地 && 0x788 无 comm 污染 → pc≥0x778 几何 erase 根本没执行 store（H2，需上游取证）。 |

## 结果如何回灌
- 每个实验产出 `/data/local/tmp/{E4a,E4b,E1}_log.txt` + 各 .out + snap——回收到
  `logs_raw/20260902_e4_pi/` 后按 CHECKPOINT §七 H 假设表归档判定，更新 PROGRESS.md。
- 若 E4b 出 root_alive：下一步走 su_daemon 落地链（mt58 以来未变），对照
  `MORNING_RUNBOOK_FINAL_2026-08-17.md`。
- 若 E4a 失败且 E1 判 H2：方向转触发链取证（为什么主树 pc≥0x778 的 erase 不执行 store），
  现有 H2 证据链是 `C1/Cfork/GeomB` 全灭 + 0x788 探针无污染。

## 关键文件索引
- `CHECKPOINT_C_stage_paradox_20260901.md` — C 悖论完整证明 + E4 推导 + 实验矩阵（§九）
- `bin/mt72/BUILD_INFO.txt` — 构建配方/SHA/验证记录
- `research/mt11_v30_src/main.c` — E4 块（~813-855 行）+ PI 重置守卫（~962 行）
- `scripts/{disasm,find_callers,find_offset_access,dump_task_fields,mt_elf}.py` — 重汇编分析工具链（已对 `ref/kernel_with_symbols.elf` 复验）
- `logs/HANDOFF_20260830.md` — 上一份交接（两阶段结构总述）
