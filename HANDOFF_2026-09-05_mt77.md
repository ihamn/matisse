# 项目交接文档 — matisse CVE-2026-43499 / GhostLock (2026-09-05 mt77)

> 写给下一个完整接管的 AI / 对面。当前现场不稳定，E5 系列已连续导致重启，
> 请先读本文 + 关键 checkpoint，不要盲目开火。

## 设备与目标
- Redmi K50 Pro (matisse), HyperOS 2.0.6.0.ULKCNXM, kernel 5.10.209-android12-9,
  KASLR=0。
- 目标：CVE-2026-43499 临时 root → KernelSU（**但用户明确：root 成功后不得用
  root 权限对手机做任何操作**；是否继续 KSU 需用户另行明确授权）。
- Shell：rish / Shizuku (uid=2000)。关键地址：
  `init_cred=0xffffff80027b0ae0`, `INIT_TASK=0xffffff800279bec0`,
  `enforcing alias pc=0xffffff8002a41b90`。

## 当前关键认知
1. **R 阶段写 `task+0x778 real_cred=init_cred` 稳定落地**（7/7），检测正常。
2. **C 阶段写 `task+0x780 cred=init_cred` 其实大概率一直落地**，但 cred 换成
   init_cred 后子进程 SELinux SID 变 kernel → enforcing 下文件/status 输出被拒 →
   检测通道被“致盲”，表现为假 0/N。见：
   `CHECKPOINT_detection_artifact_20260904.md`
3. **mt73** 已加入 sticky CapEff + `sethostname("glroot")` 无文件信标，
   尚未在“全风暴 + 无 E5 干扰”下完全验证。
4. **mt77 的 `PSELECT_CONSUMER_CPU=6` 已解决 R 饿窗**：
   R 轮 full storm 落地成功（`logs_raw/20260905_mt77_e5v2_reboot/R_mt77_landed.out`）。
5. **E5 系列（写 selinux_state 置 permissive）目前不可用**：
   - E5v1 全 8 字节清零：enforce=0 成功但黑屏/重启 ×2。
   - E5v2 保持 initialized=1（value=0x10000）：仍重启，输出全 NUL。
   - 原因未定，疑似用户态 framework/watchdog 对 enforce=0 或状态不一致的反应；
     需要 pstore/内核日志才能实锤。**未解决前不要再跑 E5**。

## 最近现场结果
| 时间 | 实验 | 结果 |
|---|---|---|
| 09-04 | mt73 E4a | 多次饿窗，无有效判定 |
| 09-05 | mt74 E5v1 | enforce=0，随后黑屏/重启 |
| 09-05 | mt75b E5 fork | enforce=0，随后黑屏/挂死 |
| 09-05 | mt77 R + CPU6 | R 落地成功 ✅ |
| 09-05 | mt77 E5v2 external | 重启，输出全 NUL ❌ |

## 代码/二进制状态
- 当前最新源：gitee master `27d06cc` (mt77 source) + 本地提交 `6e007c8`
  （现场记录，含 mt77 binary）。
- `bin/` 下已有 mt73/mt74/mt76/mt77 等；mt77 SHA：
  `5b456e3c058bf3c16484fb97304b018f4eb8cd35500a30acb79576d4bb043c37`
- 重要代码：
  - mt73: main.c child loop sticky CapEff + sethostname
  - mt74/76: `PSELECT_SELINUX_ENF`, `PSELECT_SELINUX_ENF_VALUE`
  - mt77: `PSELECT_CONSUMER_CPU` 大核 override

## 现场纪律（重要）
- 用户已把 load gate 放宽到 **15**：1min loadavg >15 拒跑。
- 屏幕常亮 + 插电；Shizuku 需手动恢复。
- 每次跑完必须清理残留：
  ```
  pkill -9 -x sleep
  pkill -9 -f 'while :; do'
  pkill -9 -f 'sleep 1'
  pkill -9 -f 'run_E1_gradient'
  pkill -9 -f 'timeout 250 env PSELECT'
  ```
- 若 E5 仍会重启，禁止继续 E5 现场轮；先做静态/pstore 分析。
- 手机曾多次出现 system_server/zygote 级软重启、黑屏无法唤醒；dsh web 的
  `task-board/storages` 也出现损坏 inode（已隔离），需 root 才能彻底清理。

## 给下一手的建议顺序
1. 拉仓库：`git pull`
2. 先看：
   - `CHECKPOINT_e5_geometry_20260904.md`
   - `CHECKPOINT_detection_artifact_20260904.md`
   - `logs_raw/20260905_mt77_e5v2_reboot/`
   - `HANDOFF_2026-09-02.md`
3. 现场若已重启，尝试 pstore/dmesg 取证 E5v2 重启原因。
4. 在 E5 根因解决前，不要继续跑 E5；可先做非 E5 的检测验证（如 R+mt77 已通）。
5. 若继续 root 链，需与用户确认是否允许 KSU/insmod；当前默认**不允许用 root
   权限执行任何额外操作**。

## 证据/日志索引
- `logs_raw/20260905_mt77_e5v2_reboot/` — mt77 R 成功 + E5v2 重启
- `logs_raw/20260905_e5_black2/` — E5 第二次黑屏
- `logs_raw/20260905_e5/` — E5 第一次成功/黑屏
- `logs_raw/20260904_off778_full/` — off778 全风暴无 euid
- `logs_raw/20260903_e1_partial/` — E1 off788 comm 污染
- `logs_raw/20260901_c_geom_decisions/` — R/C 几何判定
