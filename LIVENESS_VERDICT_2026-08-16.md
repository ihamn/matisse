# 模式 C 裁定 — 偏向纯波动，热窗待证；R0 策略改为可跳过（外部评审，2026-08-16）

> 回复 `LIVENESS_STATS_2026-08-16.md` + `FIELD_STATUS_2026-08-16.md` 四问。
> **一句话：模式 C 按纯命中率波动处理，继续跑；但每轮记负载/频率/温度，
> 用数据裁"热窗假说"。R0 从"必做闸门"降级为"诊断手段"——当前 boot 直接
> `SKIP_R0=1` 跑 STAGE-R。**

---

## Q1：纯波动还是低活性期？→ 证据偏向纯波动

三条已发生的证伪，把候选机制杀得只剩两个：

| 候选机制 | 预测 | 现场 | 结论 |
|---------|------|------|------|
| 同 boot 噪声/安静度 | 等 20-30min 后复活 | 1f3bc1bf 等后仍灭 | **死** |
| slab 污染跨 boot 持久 | 换 boot 仍灭 | 重启本就重置全部内核内存 | **不可能**（13c15d67 是干净 boot） |
| slab 同 boot 回收 | 等 cooling 后复活 | 同上仍灭 | **死** |
| 纯波动 | 6 连灭概率见下 | 吻合 | **活** |
| 热降频漂移窗口 | 灭伴随高频温/低频 | 未测 | **待证** |

概率账：历史单轮 20-40% 命中 → 6 连灭（2 boot × 3 轮）概率
`0.6^6 = 4.7%`（乐观端）到 `0.8^6 = 26%`（悲观端）。**在历史区间下沿这
根本不算异常**；且今天全天几十次触发轮，5% 量级的事件在某个时间窗聚簇是
必然会出现的（多重比较效应）。统计上不构成"regime change"的证据。

热窗假说是唯一值得留着的活候选，而且有具体机制：

```
slide.c:7  SLIDE_CONSUME_USEC 0     ← consumer 窗口走 2000×yield 自旋路径
slide.c:270  for (spin < 2000) yield ← yield 墙钟时间 ∝ 1/CPU频率
common.h:55/125  CORE=0, CONSUMER_CORE=1 ← D9000 上是 A510 小核簇，
                                          全 SoC 最先被热降频的簇
```
race 窗口是按某个频率校准的固定自旋数。下午连续重负载 + 6 次崩溃重启的
设备，小核频率一旦掉档，窗口整体漂移 → 表现恰好就是"prepare 正常、触发
正常、wrote=0"。**可测**：每轮跑前记 `loadavg + cpu0/1 cur_freq +
thermal_zone0`（脚本已加 `logenv()`）。若"灭"总伴随低频/高温、"活"总在
冷态，坐实热窗 → 对策是换时段跑，或把窗口改 usleep 模式（需重编 .so，
坐实了再动）。

## Q2：fork/spray + 崩溃重启改变了持久状态？→ 内核态不可能

重启把内核内存全部重置，slab/分配器/调度器学习态都活不过一次重启。
模式 B 的崩溃也不会"教坏"下一个 boot。能跨 boot 持久的只有：热状态、
电池、时段（系统后台活动）。前两个归入热窗假说（已给测法），第三个被
1f3bc1bf 的等待实验反证过一半。**别再往"内核持久损伤"方向投注意力。**

## Q3：策略 → 三条，按序执行

1. **当前 boot 13c15d67 不要重启**，`SKIP_R0=1 sh test_mt49_root.sh` 直接
   ENF(3) → STAGE-R(4)。理由：
   - R0✅ 按设计 = 崩一台（牺牲 boot + 脚本随之中断），低命中日这是纯浪费；
   - STAGE-R 落地是**无崩判活**（状态文件 CapEff 满帽）且本身就是进展；
   - R0 的几何（TREE_PC=...bb0, LEFT=0）只是代理，各几何命中率本就不同，
     R0 灭 ≠ STAGE-R 灭。让目标几何自己说话。
2. STAGE-R 4 轮 + 外部补打全灭 → **补一轮 R0(3轮) 做诊断**：
   R0✅ → 原语活，只是 PTR 几何没中（继续补打或下轮再试）；
   R0 也灭 → 才重启，新 boot 仍 SKIP_R0 起步。
3. **预算纪律**：每 boot 触发 ≤10 轮无落地必换；全天重启次数封顶
   （设备损耗已显性化——boot 疲劳比丢 boot 更贵）。

## Q4：bug 簇裁定 → 已推送（f189c04 `PRIOCHAIN_VERDICT`），mt49 照跑

- `+0x1788` = MTK 构建自加的 dequeue 前 `top_waiter->lock == lock` 断言
  （上游 v5.10 与 android12-5.10 均无，源码 diff 实证）；
- 我此前 rb_insert rebalance 写坏 0x770..0x788 的推演**作废**（死在 insert
  之前），已在 MT49_ADJUDICATION 挂勘误；根因（同进程二触 + 毒树）不变；
- **mt49 一进程一写 = 时序/进程/引用三重隔离，结构性免疫，不改设计**；
- STAGE-R/C 已提至 4 轮；本轮脚本再加 `SKIP_R0` + `logenv()`。
- 顺带：`errno=110`（ETIMEDOUT）是 futex 齐发的**正常未中签名**（50ms 满
  阻塞在 held word 上），不是新故障。

## 本轮脚本改动（shell-only，无需重编 .so）

| 改动 | 作用 |
|------|------|
| `SKIP_R0=1` 环境变量 | 跳过 R0 闸门，STAGE-R 自证判活 |
| `logenv()` 每轮记录 loadavg/cpu0+cpu1 cur_freq/thermal_zone0 | 裁热窗假说的数据 |

—— 外部评审（这次把概率账和热窗机制一起算给你了）
