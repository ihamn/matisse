# mt46 — mt36→mt45 全部"no root"结果的根因破案（外部评审 deepseek，8-16）

## 一句话

**mt33 以来的 cred 写入实验，写目标一直是"一个已经退出的子进程的已释放 cred"。**
子进程 fork 后毫秒级退出（futex 用法错误），父进程写入时 cred 早已被 exit_creds 释放。
写原语本身可能从头到尾都是好的——只是从来没有一个活着的靶子。

## 两个致命 bug（代码证据）

### Bug 1（致命）: FUTEX_WAIT 立即 EAGAIN → 子进程秒退

```c
uint32_t uaddr = 0;
syscall(SYS_futex, &uaddr, FUTEX_WAIT, 1, NULL, NULL, 0);
                          /* expected val = 1, 但 *uaddr = 0 */
```

futex 语义: FUTEX_WAIT 先比较 `*uaddr == expected`，不等 → 立即返回 `-EAGAIN`。
0 ≠ 1 → **子进程根本没阻塞**，直接落到 id 检查 → 非 root → `_exit(2)`。
时序: fork → pipe 写 task/cred → futex EAGAIN → exit(2)，全程毫秒级。
父进程随后（≥0.5s 后）才发起写入 → 子进程 cred 已经过 exit_creds → put_cred → usage 1→0 → RCU 释放，
写目标 = 已释放/已复用的 slab 对象。

**这解释了 mt36-45 的全部 "no root"，也顺带解释了为什么多数情况"不崩"**
（零写一个已释放对象通常无害；偶尔复用成别的对象就崩，如 mt29 attempt3）。

### Bug 2（叠加）: 跨进程唤醒结构性不可能

```c
/* child */  syscall(SYS_futex, &uaddr, FUTEX_WAIT, ...);   // child 的栈
/* parent */ syscall(SYS_futex, &wu,     FUTEX_WAKE, ...);   // parent 的栈, 另一个变量
```

私有内存 futex 的 key = (mm, uaddr)。fork 后父子 mm 不同、变量地址也不同
→ 父进程的 FUTEX_WAKE **永远**叫不醒子进程。就算 Bug 1 修了（val 匹配真睡），这套唤醒也无效。

## mt46 修复（已打入 main.c，3 处）

1. **子进程自检环**: 保持存活，200ms 周期查 6 个 id 字段（getresuid/getresgid），
   任一为 0 → `CHILD-ROOT` + `_exit(42)`；10s 心跳打印；3 分钟超时兜底。不再依赖任何唤醒。
2. **删掉无效的 FUTEX_WAKE**（跨 mm 唤醒不可能）。
3. **窗口轮扫加 `PSELECT_UID_WIN` env 覆盖**:
   配 `PSELECT_RETRY=1` + 每轮新进程（mt25/26 已验证纪律），窗口由脚本按轮指定——
   规避进程内连续重试导致的 trigger-110（mt30/mt44 的故障模式）。

## 窗口语义修正（ELF 反汇编证实）

STORE(a) `[parent+8] = child` 是 **64 位**store → 一次干净零写清掉**一对**字段：

| 窗口 | 零写覆盖 | 可观测性 |
|------|----------|----------|
| 0x04 | uid + gid | getuid/getgid → 0 ✓ |
| 0x14 | euid + egid | geteuid/getegid → 0 ✓ |
| 0x1c | fsuid + fsgid | 六 id 不可见（setfsuid(-1) 可查）|
| 0x24 | securebits + cap_inheritable.lo | 纯对照组 |

所以轮扫里 3/4 的窗口直接可判；0x1c/0x24 是对照。若要 8 个 id 全清，后续补 0x0c（suid+sgid）。

## 重跑方案（设备恢复后）

`scripts/test_mt46_p0a.sh`（重建 .so 后执行）:
- round 0 判活: 非 CRED 路径写 boot_id（mt25 配方）→ boot_id 首字节变 00 = 本 boot 原语活着
- round 1-8: CRED 路径 × 窗口轮扫（每轮新进程 + RETRY=1）

### 判读表

| 结果 | 结论 | 下一步 |
|------|------|--------|
| R0 变 + 某轮 CHILD-ROOT | **P0-A 裁决成功**: uid@+0x4 铁证 + cred 零写落地 | 直接扩到全 id 清零 + mt26 permissive 配方组合 |
| R0 变 + 8 轮全灭 | CRED 路径触发问题（fork/perf 扰动悬垂链）| 查 trigger 链: 对比 CRED/非 CRED 路径 futex trigger 返回值 |
| R0 不变 | 本 boot 原语未激活（slab 状态差）| 换 boot 重跑，别浪费 cred 轮 |

## 附带影响（历史结论修订）

- mt44 "TREE_PC 主树写 cred+0x4=0 → 写不落地" 的结论**作废**——写可能落了，
  只是落在已释放 slab 上，且子进程检测通道当时也是死的（秒退）。
- mt36-45 所有"no root"实验**不构成**对"零写 cred 路线"的否定证据。
- mt28 系列"假 cred 无 root"部分同病（部分是真实崩溃，另案）。
- ELF_ANALYSIS_2026-08-16.md 中的两步 root 配方（cred 指针换 init_cred）仍备选，
  但优先走零写 id 路线（无 put_cred BUG 风险，mt32-36 的教训）。

## 待办（给监工的清单）

1. 设备恢复 Shizuku → 重建 .so（含 mt46 补丁）→ 跑 test_mt46_p0a.sh
2. 仍缺: mt25/mt28 时代 .so 二进制（定当时默认单词值）— 不阻塞 P0-A
3. 仍缺: pstore/dmesg 崩溃转储（验证崩溃机理叙述）— 不阻塞 P0-A
