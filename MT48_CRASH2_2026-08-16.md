# mt48 PTR 崩溃 #2 — stage=R 后崩, 无 ROOT-SEEN, 非 commit_creds BUG (2026-08-16)

> 对应您 MT48_REDESIGN 判读表第二行: "崩了, 且 stage=R 的 attempt 后无 ROOT-SEEN
> → 写本身之外还有触发路径崩溃 (我机制之外的第二个妖)"

## 前置 (全部成功)
- R0: 704bd8d0 (bytes[3..10] 清零) round1 判活 ✓
- ENF: 4 轮全灭 (Enforcing 保持, selinux_state 零写未落地 — 新卡点, 已知)
- PTR mt48: ko found

## PTR 崩溃日志 (stage=R 之后)
```
mt48: PTR stage=R task=ffffff8236290000 pc=ffffff8236290770 right=ffffff80027b0ae0
mt28m: cred write attempt 1/6 fake_cred=ffffff80027b0ae0
mt33: child pid=27923 task=ffffff8236290000 blocking-for-cred-write
mt47: alive poll=50 uid=2000 CapEff=0000000000000000
```
→ 日志在此戛然而止。崩溃在 stage=R 写后、stage=C 之前 (或 C 写时)。

## 关键事实
1. **stage=R 写执行了** (换 real_cred), 子进程 poll=50 还活着 (CapEff=0 = 半程态正确)
2. **半程态 gate 没触发** (CapEff=0 && uid=2000 → gate 假 → 没进 setresgid) — 您的 AND-gate 工作正常
3. **所以崩溃不是 commit_creds BUG_ON** (没走到 setresgid)
4. 崩在 "R 写后" — 可能是: stage=C 的 STORE(a) 写 [task+0x780] 时崩? 或 erase 后路径崩?
5. 无 root_alive / ksu_done (PTR 未完成)

## 与上次崩溃 (旧 PTR, stage 无 R) 的对比
- 上次: pc=task+0x778 (直接换 cred) → 子进程 gate 命中 → setresgid → commit_creds BUG → 崩
- 这次: pc=task+0x770 (先换 real_cred) → gate 没命中 → 但仍在 R 写后崩

## 请求裁定
1. stage=R 写 [task+0x778]=init_cred 本身会触发什么? (real_cred 被换, 但 cred 还是旧的)
   - 半程态: current_cred() 读 cred(旧) → 正常; 有没有别的内核路径读 real_cred 且会崩?
   - 例如: /proc/self/status 的 CapEff 读 __task_cred → real_cred? 子进程 poll 正在读 status!
2. 崩溃是确定性还是概率性? (7/7 旧签名已撤回, 这次是 stage=R 新几何)
3. 是否需要: stage=C 用**单独进程** (每 stage 新子进程, 避免半程态跨轮存活)?
   还是保持进程内交替 (对面设计) — 如果 R 写后内核路径就有问题, 进程内交替本身危险?
4. 或者: 两写都用**同一轮内的两个 STORE** (R 和 C 的 pc 只差 8, 能否一次触发写两个)?

## 设备侧
- 新 boot a10e6984, Enforcing, Shizuku 需重开
- mt48 .so + 脚本已部署 (脚本 SRC 已指向 preload_mt48.so)
- 按您判读表: "停手, 日志推我, 我出 TREE_LEFT successor 几何" — 我们已停手

—— matisse 现场 (2026-08-16 13:30)
