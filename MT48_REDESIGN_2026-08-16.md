# mt48 两轮换指针重设计 — 7/7 崩溃机制指令级定罪 + 修复已实施（外部评审，2026-08-16）

> 回复 `PSTORE_NA` 三问 + `STOP_PTR_AWAIT_REDESIGN`。pstore 不需要了——
> **崩溃机制已经在反汇编里定罪，不需要崩溃栈。** 修复（mt48）已写好推上仓库。

---

## 一、先认个错：我上一轮的"7/7 统计签名"是错误归因

"pc=slab + right≠0 = 7/7"把两类不同配置混为一谈了。真机制比那个签名干净得多，
而且是**确定性的、指令级的**。mt32-36 和今天的崩溃不需要统计推理——直接看代码。

## 二、定罪：崩溃的不是写，是子进程的 setresgid（指令级铁证）

### 证据链（每步可复核）

**1. `commit_creds` 入口就是这个 BUG_ON**（目标内核实际反汇编，`analyze17.py` 可复跑）：

```
commit_creds @0xffffffc00818514c:
  ldr  x19, [x20, #0x778]     ; old = task->real_cred
  ldr  x8,  [x20, #0x780]     ; task->cred
  cmp  x8, x19                ; ★ BUG_ON(task->cred != task->real_cred)
  b.ne #0xffffffc008185530    ; → brk #0x800  ← BUG, 已验证
```

**2. 旧 PTR 设计只换一个指针**：pc=task+0x778 → STORE(a) 只写 `[task+0x780]`(cred)。
real_cred(+0x778) 保持旧 cred。**从此刻起这个 task 处于"指针不一致"地雷态。**

**3. 子进程旧 gate 在半程态必然命中**（main.c 旧行 590-607，今天出事的就是它）：

```c
getresuid(&ruid, &euid, &suid);        // 读 current_cred() = cred(已换!)
...
if (euid == 0 || ...) {                // init_cred.euid = 0 —— STORE(b) 只碰 usage+uid,
                                       // euid 完好 → 半程态 euid==0 为真!
    setresgid(0, 0, 0);                // → prepare_creds(安全) + commit_creds
    setresuid(0, 0, 0);                // → cmp cred(init_cred) vs real_cred(旧) → 不等
                                       // → brk #0x800 → panic_on_oops → 重启
```

**4. 时序完全吻合**：子进程 200ms 周期轮询 → 写落地后 ≤200ms 内引爆 → 人眼看到的就是
"写执行瞬间崩"。mt32-36 是同一结构（只换 cred + setresuid 自愈），7/7 不是巧合，是必然。

**5. 为什么日志里没有 ROOT-SEEN**：那行有 fflush，但 **fflush 只到页缓存，panic 重启
页缓存即丢**（fflush ≠ fsync）。前面几行能活下来是因为落盘时间更早/更久。这解释了
"子进程没打印就崩"的表象。（已在 mt48 里给 ROOT-SEEN 加了 fsync，崩了也留证。）

**6. 顺带解释了所有历史幸存者**：mt28b（marker 写不碰任何 task 的 cred）、mt25/26/46
（零写不换指针）、R0/ENF（同前）——没有任何一个"换指针+子进程会 setresgid"的组合活过。
一个机制统一全部 14 次实验结果。

### 我上一轮裁定里对的部分和错的部分

| 上轮结论 | 本轮修正 |
|----------|----------|
| RO 假设判死（init_cred 在可写 .data） | ✓ 维持，ELF 边界符号铁证 |
| rb_erase 两个 STORE 都落可写内存、本体不 fault | ✓ 维持 |
| "7/7 = pc=slab+right≠0 统计签名" | ✗ 撤回——错误归因 |
| 崩在 erase 之后 | ✓ 维持——但具体位置是**子进程 200ms 内的 commit_creds 入口** |

## 三、回答你们的三个问题

### Q1："8/8 即停"判别标准还成立吗？
**作废，不需要了。** 统计判别是为"机制未知"设计的；现在机制已知且修复已交付。
旧设计（单发换 cred）无论第几次跑都会崩，不存在"8/8 才确定"。直接上 mt48。

### Q2：几何重设计
你们提的三个方向都裁掉，换我的方案（已实施）：

| 你们的提案 | 裁定 |
|------------|------|
| right=0 走 mt46 安全零写 + 换等价可写目标 | ✗ right=0 时 STORE(a) 写 0：写 [task+0x780]=0 → cred=NULL → 子进程下一次任何 cred 解引用立即崩，比现在还快 |
| 先写 task+0x780 的右子为 0 再补写 init_cred | ✗ 几何上不成立：right 就是写入值本身，没有"先清右子"这个操作；且 [task+0x780]=0 同上 |
| 写 task+0x780 低 8 字节为 0 | ✗ 同第一条，零写指针=清空指针 |

**mt48 方案：两个指针都换，门控升级（已写好推上仓库）**

```
STAGE=R: pc = task+0x770 → STORE(a) [task+0x778] = init_cred   (real_cred)
STAGE=C: pc = task+0x778 → STORE(a) [task+0x780] = init_cred   (cred)
两轮都落地 → real_cred == cred == init_cred → commit_creds 的 cmp 相等 → BUG_ON 通过
```

子进程 gate 换成**满帽 AND-gate**（防半程态引爆的关键）：

```c
gate = (CapEff >= 0x000001ffffffffff) && (euid == 0);
// CapEff 读 real_cred(status 用 __task_cred) — 只有 R 落地才满帽
// euid   读 cred(getresuid 用 current_cred)   — 只有 C 落地才为 0
// 半程态(只换其一)两个条件必然一假 → gate 不触发 → BUG_ON 永不引爆
// 且"满帽"检查免疫 shell 自身 caps(adb shell CapEff 顶多几个位, 凑不出 41 位全 1)
```

半程态安全性（任一先后顺序都安全）：
- 只 R 落地：real=init(满帽)、cred=旧 → euid=2000 → gate 假 ✓
- 只 C 落地：cred=init、real=旧 → CapEff=shell 帽(不满) → gate 假 ✓
- 半程态期间子进程退出：exit_creds 无 BUG_ON（只有 commit_creds 入口有）✓
- 半程态期间子进程只做只读 syscall（getresuid/getresgid/fopen status），不会触发 commit_creds ✓

两写必须落**同一个 task** → 不能每轮换进程（换了进程就换了子进程就换了 task）。
mt48 用进程内 retry 交替：`PSELECT_PTR_ALT=1 PSELECT_RETRY=6`，一个进程里 R,C,R,C,R,C
打同一个泄漏 task，marker 检查已在循环里（落地即停）。脚本已改。

### Q3：替代证据源
- logcat/last_kmsg：panic 重启全丢，不可用（你们已验证）
- **panic_on_oops=0**：若你们有任何 root 写权限（Shizuku 若是 root 拉起的），
  `echo 0 > /proc/sys/kernel/panic_on_oops` 可把崩溃降级为可存活的 oops → dmesg 直接
  留全栈，pstore 整个问题蒸发。没有 root 写就算了
- **其实已经不需要了**：机制静态定罪 + mt48 修复后，崩溃栈的价值只剩验证。真跑崩了，
  新日志（含 fsync 加固）也能区分（见下表）

## 四、mt48 预期结果判读（跑完对号入座）

| 现象 | 含义 | 动作 |
|------|------|------|
| `mt47: ROOT-SEEN ... CapEff=000001ffffffffff` + root_alive.txt | **两个指针都换了，commit_creds 通过，root 拿到** | 终局：insmod → ksu_done.txt → 项目毕业 |
| 崩了，且日志里 stage=R 的 attempt 后无 ROOT-SEEN | 写本身之外还有触发路径崩溃（我机制之外的第二个妖） | 停手，日志推我，我出 TREE_LEFT successor 几何 |
| 不崩，2 轮×6 attempt 无 marker | 落地率问题（触发命中低） | 看 `mt48: PTR stage=` 行确认交替正常，换 boot 重跑 |
| ROOT-SEEN 出现但 root_alive.txt 没写出来 | setresgid 后崩（不该发生——cmp 已相等） | 日志推我 |

## 五、执行清单

1. **重建 .so**（main.c 改了：ALT/STAGE + 满帽 gate + fsync）
2. 当前 boot 704bd84c R0 已 3 轮全灭 → 按纪律换 boot，新 boot 跑完整脚本
   （R0 判活 → ENF 翻 Permissive → mt48 PTR 交替两轮）
3. 顺手：若 Shizuku 是 root 语境，先 `echo 0 > /proc/sys/kernel/panic_on_oops` 上保险
4. 无论结果如何，当天推仓库——成了是毕业照，崩了是罪证，都不白跑

—— 外部评审（这次连 BUG_ON 的 brk 立即数都验了：#0x800）
