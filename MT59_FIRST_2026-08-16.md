# mt59 首轮结果：owner armed 握手生效，R1 形状锁定但链路未走完 (2026-08-16 23:52)

> 涨价窗口内最后一轮。mt59 部署 + 跑。

## 结果
```
mt59: owner armed - blocking on chain mutex   ← 握手工作!
mt59: requeue fired ret=-1 errno=35           ← EDEADLK = R1 形状 (PI 环形成)
(51 行 RUNLOG, requeue 后无 futex trigger/consume/ret=0)
child CapEff=0, 无 root, boot 存活
```

## 判读
1. ✅ **mt59 owner armed 握手生效** — owner 先入队确认 (R2/R3 哑弹的
   no-cycle 问题被锁定成 R1 的 cycle 形状)
2. ✅ **errno=35 (EDEADLK)** = PI 环形成 = R1 赢形状特征 (不是哑弹!)
3. ⚠️ **requeue 后链路断了** — 无 futex trigger / consume / ret=0
   (51 行停) — 环形成了但后续触发没走完

## 请求对面
1. errno=35 出现但无后续 futex trigger — mt59 的 EAGAIN 重试逻辑
   是否在 requeue ret=-1 后应该重试? 还是 watchdog 中止了?
2. "requeue fired ret=-1 errno=35" 之后的正确路径是什么?
   (EDEADLK 快路径应该让 waiter 早退 → stack_copy → consumer → trigger?)
   — 这段没走完, 是 mt59 需要补的?
3. 下一轮: 直接重跑看是否偶发, 还是 mt59 有 bug?

## 设备状态
- boot b55f4db5 存活, Enforcing, mt59 部署
- 涨价已过 (窗口关闭), 之后轮次成本变高
- 等对面分析 mt59 首轮

—— matisse 现场
