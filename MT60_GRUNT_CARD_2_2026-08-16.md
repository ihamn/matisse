# mt60 零思考执行卡 · 第 2 弹（补证 + 部署 mt60 + R5）2026-08-16

> 规则同第 1 弹：按编号复制命令 → 原样贴回全部输出 → 不一致就停。
> 判断全在评审侧。rc 是几都照贴，不许自行重跑。

---

## 第 1 步 · 补推 R4 原始证据（上一弹漏推的，必须补）

```sh
tail -30 /data/local/tmp/R4.out
wc -l /data/local/tmp/R4.out
```

整段贴回（不许 grep、不许摘要）。评审要看 `mt59: STALL` 行在不在、
`mt59: requeue fired` 之后到底还有没有字。

## 第 2 步 · ANR 证据（上轮弹了 3 个"系统无响应"）

```sh
ls -la /data/anr/
```

贴回目录列表。然后取**最新 3 个文件**，每个推仓库一份原文（不裁剪）：

```sh
cp $(ls -t /data/anr/* | head -3) /sdcard/ 2>/dev/null || true
```

（能直接推仓库就直接推，上面只是兜底；文件大就原样大着推，不许截断。）

## 第 3 步 · 部署 mt60（替换 mt59，su_daemon 不动）

```sh
cp bin/mt60/preload.so /data/local/tmp/preload.so
echo "cc990983712626dca12e405f887c4cbc18929b96c2c26c2e003e627cbeff02a4  /data/local/tmp/preload.so" | sha256sum -c
```

- `preload.so: OK` → 进第 4 步
- `FAILED` → 停，贴回两行

## 第 4 步 · 体检（整块复制）

```sh
date; cat /proc/sys/kernel/random/boot_id; getenforce
awk '{print ($1>600)?"SETTLE_OK":"SETTLE_WAIT"}' /proc/uptime
awk '{print ($1<3)?"LOAD_GO":"LOAD_WAIT"}' /proc/loadavg
ps -A | grep -E "sleep|preload" >/dev/null 2>&1 && echo RESIDUE_DIRTY || echo RESIDUE_CLEAN
```

三旗 = `SETTLE_OK` + `LOAD_GO` + `RESIDUE_CLEAN` → 第 5 步；否则停，整块贴回。
（ANR 之后系统需要沉降：若 `SETTLE_WAIT`，等 10 分钟后重跑本步，再不行就贴回来等评审。）

## 第 5 步 · 开火 R5（命令一个字不改）

```sh
am kill-all
timeout 220 env \
  PSELECT_SLIDE_TRIGGER=1 \
  PSELECT_CRED=1 PSELECT_PERF_CRED=1 \
  PSELECT_RETRY=1 \
  PSELECT_PTR_MODE=1 PSELECT_PTR_STAGE=R PSELECT_PTR_STRICT=1 \
  PSELECT_PTR_RIGHT=auto \
  PSELECT_TREE_PC=ffffff8002a41b90 PSELECT_TREE_LEFT=0 \
  PSELECT_SKIP_WARMUP=1 \
  PSELECT_WAIT_SECONDS=200 \
  LD_PRELOAD=/data/local/tmp/preload.so \
  /system/bin/sleep 180 > /data/local/tmp/R5.out 2>&1
echo "R5 rc=$? enforce=$(getenforce) boot=$(cat /proc/sys/kernel/random/boot_id)"
```

注意：`PSELECT_WAIT_SECONDS=200` 这行**保留不动**（mt60 里它对 waiter 已失效，
留着是为了对齐历史配方；waiter 唤醒已由二进制内部 3s 默认接管）。

- rc=`0` / `3` / `124` / 其他：都照贴，全部是"正常待审"。
- 本轮新看点（不用你判断，推上来就行）：
  `mt60: pre-requeue words`、`mt60: waiter FWRQ`、`mt60: STALL flags` 这三行
  出不出现、值是多少，评审要。

## 第 6 步 · 活体确认 + 推送

```sh
getenforce; echo "alive_rc=$?"
cat /proc/sys/kernel/random/boot_id
```

推仓库四样，一样不许加工：
1. `/data/local/tmp/R5.out` 原样全文
2. 第 5 步 `R5 rc=...` 回显行
3. 第 6 步两行回显
4. （如弹了 ANR/黑屏）`getprop ro.boot.bootreason` + ANR 目录最新文件

推完**停**。第 3 弹卡由评审出。

---

## 事故口令（同第 1 弹）

黑屏/无响应：短按电源 ×2 等 60 秒 → 不行长按。恢复后只推
`.out` 现状 + `ro.boot.bootreason`，然后停。

## 禁止事项（同第 1 弹）

不重跑、不改参数、不总结日志、不加跑轮次、不自行"再试一次"。
