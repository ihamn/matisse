# 立即执行：90 分钟窗口作战序列（取代"明早"，2026-08-16 深夜）

> **本文件覆盖 MORNING_RUNBOOK 的时间设定**：窗口 = 现在 → T+90min。
> 命令全部复用总册（§二 R / §三 C / §四 A1），这里只给时间表和截止规则。
> 设备自 21:07 后一直闲置 = 已远超沉降要求，**不需要再等 10 分钟**。

---

## 时间表（从按下第一行命令起算）

### T+0 ~ T+8：起飞（并行做）
```
cp bin/mt58/preload.so /data/local/tmp/preload.so
sha256sum /data/local/tmp/preload.so   # 必须 == b6d4cf91...a8e3621，不对=停
date; cat /proc/sys/kernel/random/boot_id; getenforce
cat /proc/loadavg; grep MemAvailable /proc/meminfo
```
- loadavg > 3 → 等 3 分钟再开（只此一次）
- 插上充电器（历史胜局条件之一，无害）

### T+8 ~ T+30：R 直打（≤3 轮）
- **R1 立即**（命令=总册§二，二进制已是 mt58）
- 每轮后 10 秒三查（总册§二判读表**原样适用**）
- 干净 miss → 间隔 120s → 下一轮
- **R 落地 → 跳总册§三**（60s 静观 + 收割 + C）
- `perf task leak failed` → 转 A1 分支（下条）
- `CANARY CORRUPTED` / 脏轮 → 停 5min 查 getenforce+dsh，再续（只容忍一次，第二次=收工）

### 分支 A（perf 被拒，T+30 ~ T+70）：A1 翻盘 → R
- A1 胜局配方 ≤2 轮（历史 round1 即翻），轮间隔 120s + 三查
- 翻 Permissive → 立即 R（此时 perf 已通）
- **T+70 还没翻 → 放弃，收工推证**（时间不够再赌）

### 分支 B（全部 miss）：T+30 后不再加轮
- 3 轮 R 干净 miss = 今晚数据已够，**不加第 4 轮**
- 直接进收尾

### 硬截止
- **T+75：无论在哪，停止开新轮**（在跑的让它跑完）
- T+75 ~ T+90：收尾推送（总册§五清单）

## 今晚专属预算（比白天紧）

| 项 | 预算 | 触发后 |
|---|---|---|
| 冻结 | **1 次**（今晚用户已长按过两次） | 用户协议梯子 → 恢复后 **直接收工**，只推证不续跑 |
| 有栈崩溃 | 1 次 | pstore 原文 + .out 推仓库 → 停 |
| CANARY/脏轮 | 共 2 次 | 第 2 次 = 收工 |

## 一句话版本（如果只记住一条）

**R1 现在就打，判读照总册表，T+75 停，冻结一次就收工。**

—— 评审 · 时间污染已清除：没有明早，只有现在 → T+90
