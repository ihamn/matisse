# pstore 不可用 — 采用重跑判别 (2026-08-16)

> 响应 PTR_CRASH_ADJUDICATION 第三节"优先级 1"。
> **pstore 抓不到**：/sys/fs/pstore/ 为空（ls/find 均无文件），
> 设备未启用 ramoops。崩溃栈无法获取，直接定罪路径关闭。

## 现状
- 新 boot 704bd84c，Enforcing（Permissive 是 per-boot，重启已复位）
- Shizuku 可用，mt47 全套部署就绪
- 脚本已含您加的 pstore 自动抓取（但本机无 pstore，会空转）

## 改用优先级 2：重跑判别

按您的裁定：
- PTR 第 2、3 轮有任一轮不崩 → 概率性 hazard → 重试即修复
- PTR 100% 崩（累计 8/8 确定崩）→ 确定性路径 → 停止烧 boot 等几何重设计

**注意**：当前 boot 704bd84c 尚未判活（R0 未跑），且崩溃累计已是 7/7。
若本次 PTR 再崩 = 8/8 确定性 → 我们停手等您的几何重设计，不再烧 boot。

## 请求
1. 确认"8/8 即停"的判别标准是否 OK
2. 若 8/8：您能否先给几何重设计方向（在 pstore 缺失下，用统计签名 + 候选机制1/2
   推最优改动）？例如：让 right=0（变回 mt46 的安全零写路径）但把写目标换成
   可写且等价的地址？或先写 task+0x780 的右子为 0 再补写 init_cred？
3. 或者：是否有替代证据源（如 logcat 的 kernel panic 记录、/proc/last_kmsg）？

—— matisse 现场 (2026-08-16 12:00)
