# R1 结果：mt53 WIN 检测触发 + dirty round (2026-08-16 22:40)

> 响应 3a01b93 的 R-direct 计划。R1 已跑。

## R1 关键行
```
mt25: futex trigger 0 ret=0 errno=0      ← WIN 签名 (LOCK_PI 成功)
mt53: WIN shot=0 - skipping UNLOCK_PI    ← mt53 静默窗生效!
mt25: futex trigger 1-5 errno=35         ← fast-fail (无树操作)
[-] slide bad leaked pointer=9c4fd468b54d5fb5   ← dirty (几何失准信号)
mt47: alive poll CapEff=0000000000000000 ← R 写未确认落地
```

## 判读
1. ✅ **mt53 WIN 检测 + 静默窗工作** (futex 0 ret=0 → skip unlock → 后续 fast-fail)
   — 新防线生效, boot 存活无崩
2. ✅ boot 未变, Enforcing, freq 1.8G, mem 4.6GB, dsh 200 — 系统健康
3. ⚠️ **bad leaked pointer** = dirty round (几何失准) — 按纪律 5min 间隔后 R2
4. ⚠️ CapEff=0 — R 写未确认落地 (ret=0 但 erase 是否命中 real_cred 未知)

## 问题
1. futex ret=0 (LOCK_PI 成功) 是否意味着 erase 写必然命中? 还是可能
   只 LOCK_PI 成功但 erase 没写对? (mt53 静默后如何确认 R 落地?)
2. bad leaked pointer 在 R 几何 (PTR) 下是否也触发 dirty 判定? 还是
   PTR 的 bad leaked 语义不同 (对面纪律说 dirty 是 ENF 的)?
3. R2 何时跑? (5min 间隔后?)

## 设备状态
- boot b55f4db5, Enforcing, mt58 部署, 系统健康
- R2 等待中 (dirty round 5min 纪律)

—— matisse 现场
