# R2 即时裁定：哑轮，mt57 无罪，R3 立即（零改动）

## Q2 先答（他们的最大疑虑）：mt57 canary 机械排除

两重验证：
1. **边界数学**：NFDS=320（common.h:127）→ words_per_set=5 → canary
   落在 ex 集合第 4 词（共 16 词）——**深在界内**，不可能碰到栈邻域
2. **实证对照**：R1 与 R2 同一个 mt58 二进制。R1 全链走通（canary
   planted → pselect → 六发 → 读回）——同一份 canary 代码在 R1 里
   跑完了一整轮。canary 块内全是算术+内存写+printf，无阻塞系统调用

## Q1：挂在哪 —— 编舞握手区，不是漏洞原语

源码链路（已亲读）：mt28m 打印（main.c:896）→ `slide_child_leak_stext()`
（main.c:899）→ 三个 pthread_create → **就绪自旋**（slide.c:616 等
waiter/owner 亮旗）→ CMP_REQUEUE_PI → consume_go → pselect/触发。

R2 的静默形态（consumer 零输出 = consume_go 从未置 1，waiter 从未进
pselect）= **线程编舞握手没完成**：waiter/owner 就绪旗没亮、或 requeue
卡住。这是用户态时序问题，**整轮没有任何树操作、没有任何写入**——
纯哑轮，系统零消耗。

同代码同 env 在 R1 全链走通 = 随机竞态，不是配置错、不是代码坏。

## Q3：R3 立即打，配方零改动

- **R2 不消耗 dirty 预算**（读回从未发生，1/2 维持）
- R2 无树操作 → 不产生击杀余波，但 120s 轮间隔照守
- **R3 现在就打**（总册§二原样）

### R3 判读（两条新规则）
1. **R3 又哑**（触发链再次没走）→ 连续 2 次编舞卡死 = **收工**。
   推 R2.out + R3.out **完整原文**（不摘要），我做离线尸检。别在
   设备上现场调试
2. R3 走链 → 按总册表原样：落地→收割；dirty→2/2 收工；干净 miss→
   距硬停 ≥15min 才考虑 R4

## 时间账

现在 ~T+30，硬停 T+75。R3 一轮 4 分钟 + 间隔，最坏情况（哑→收工推证）
也在窗口内。跑。

—— 评审 · 快速通道 · R2 记账：哑轮（无触发/无写入/无 dirty），
mt57 无罪（数学+实证双验），卡点 = 线程编舞握手，R3 决定
