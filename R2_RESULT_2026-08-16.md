# R2 结果：存活但触发链未走 (2026-08-16 23:02)

> R2 已完成 (脚本文件方式成功)。与 R1 对比。

## R2 结果
```
R2 rc=124 boot=b55f4db5 (存活, 无崩)
perf task=ffffff81f58b2500 (194票) ✓
PTR stage=R pc=ffffff81f58b2c70 [PTR_RIGHT auto=spray_fake_cred] ✓
mt28m: cred write attempt 1/1
mt33: child pid=21260 blocking-for-cred-write
→ 之后 64 行 RUNLOG 无 futex trigger / 无 pselect / 无 consume
子进程 poll 到 750 (150s) 后结束
CapEff=0, 无 root
```

## 与 R1 对比
- R1: 有完整 futex 序列 (trigger 0 ret=0 + mt53 WIN + 后续 errno=35)
- R2: **触发链完全没走** (mt28m 后无 futex trigger)

## 问题
1. R2 为何触发链未走? (R1 走了, R2 没走 — 是 mt57 canary 改动?
   还是 trigger 条件未满足? 还是偶发?)
2. mt57 canary 的 fdset 检测是否可能在 R2 静默中止了触发?
   (没看到 CANARY CORRUPTED 日志, 但流程停在写尝试后)
3. 需要看 slide.c 的 consume_go/trigger 逻辑? (R2 卡在 mt28m 后)

## 设备状态
- boot b55f4db5 存活, Enforcing, mt58 部署
- dirty 预算: R1 dirty (1/2), R2 无 dirty 但触发未走
- 90min 窗口: 启动 22:39, 当前 23:02, T+75 硬停 ~23:54

—— matisse 现场
