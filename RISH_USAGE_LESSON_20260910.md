# rish 正确用法 (2026-09-10 实测教训)

## 结论
- **必须**  ./rish -c "命令"   或   echo "命令" | ./rish   (stdin 模式)
- **不能**  ./rish "命令"   ← argv 模式**静默无效**: 无输出、rc=0,
  极易误判成 "Shizuku 挂了/超时", 实际是调用姿势错误.

## 实测 (同一台机, 同一时刻)
| 调用 | 结果 |
|---|---|
| ./rish id -u | (空) rc=0 |
| ./rish -c "id -u" | **2000** rc=0 |
| echo "id -u" \| ./rish | **2000** rc=0 |
| ./rish sh -c "id -u" | (空) rc=0 |

## 影响与修复
- root_strike.sh / load_ksu_v2.sh / 我写的 ksu_load.sh 原来都用 argv 模式
  → 全部静默空跑 (这就是"脚本跑不动"的真相).
- 已全部改为 -c 模式; ksu_load.sh v3 增加 **rish 自检**: id -u 必须回 2000,
  否则立即退出并给出排查步骤 (不再静默).
- ksu_hunt.sh 自带 args/stdin 探测, 会回退到 stdin 模式, 未受影响 (同样建议统一 -c).

## 其它同步修正 (ksu_load.sh v3)
- load 门限 15 → 25: 本机 MIUI 基线负载就是 ~16 (16 核全饱和, 零残留实证),
  旧门限会把正常机器永久挡住.
- D 态进程不再拦截 (含 kworker 等内核线程, 属正常), 仅打印.
- 新增 "开机 > 6 小时 → 先重启" 门 (毒节点清零纪律).
