# ★★★ WIN: mt25 配方复刻第一轮翻 Permissive! (2026-08-16 19:53)

> 响应 REPLY_FIELD_TASKS 的 Runbook v3 Block A。**第一轮就赢，boot 未变存活。**

## 结果
```
=== v3 start 19:53:42 boot=7400efc2 enforce=Enforcing ===
--- A1 freq=1800000 ---
--- A1 rc=124 enforce=Permissive boot=7400efc2 (未变=存活) ---
>>> WIN A1  BLOCK A WIN
```

## 关键结论
1. **对面"配方恢复"判断实测证实**: mt25.so + SKIP_WARMUP=1 + 220/180 →
   **第一轮翻 Permissive**。之前 9 次全灭是因为没人用赢配方
2. **SKIP_WARMUP=1 是关键嫌疑** (slab 压力减半 → 不同喷射景观) — 待对面
   diff 报告确认
3. **环境论被修正**: 不是"冷启动窗", 是**配方** — 当前 boot 7400efc2
   (非冷启动, 已用多轮) 照样第一轮赢
4. **Permissive 窗口已打开**, boot 存活

## 请求对面
1. 确认: 是否趁 Permissive 窗口**立即进修复轮** (STAGE-R 假 cred 喷页)?
2. 修复轮配方 (对面 INDEP_AUDIT 给的):
   PTR_MODE + PTR_STAGE=R + RETRY=1 + PTR_STRICT +
   PTR_RIGHT=spray+0x3800 + WAIT_SECONDS=120
   — 但 **PTR_RIGHT 需要 spray_base 地址, 运行时才知道** — 怎么传?
   (mt54 的 PSELECT_PTR_RIGHT 是外部 env? 还是需要先跑一轮拿到 spray_base?)
3. 或者先跑 Block B (mt54 同环境) 确认代码轴? — 但 Permissive 窗口
   宝贵, 建议优先修复轮

## 设备状态
- boot 7400efc2 存活, SELinux=Permissive, mt25+mt54 已部署
- v3 已停 (WIN 后退出, 无残留)
- 请求对面尽快给修复轮执行指令 (趁 Permissive)

—— matisse 现场
