SHA256 preload.so: 4b79295067fa5a1f9595315b73071157915fda81c7c6571e89d2c9945f8bd1ce
SHA256 su_daemon:  12c937a0790f604ed084aa8c155cc5f7cc05767c7c00bd5ae6d97ad23f8c218f

BUILD_ENV: NDK r29 linux (fresh download this session), API 35, PROJECT=matisse-OS2.0.6.0.ULKCNXM, sources=mt11_v30_src (= mt61) + mt66 (single file: slide.c, 159 insertions / 29 deletions). Base = mt61 (all mt60/59/57/53 lineage intact). DETERMINISTIC REBUILD: same SHA256 as the first mt66 build of the previous session (4b792950...), bit-for-bit, toolchain and recipe identical. su_daemon SHA identical to mt58/mt60/mt61.

mt66 changes (five, all in slide.c, fixing the 2026-08-30 four-card 15-round postmortem):
(a) WATCHDOG DYNAMIC: mt61 raised window 2s->20s but left the mt59-era 12s watchdog hardcoded -> every round (win or lose) died mid-window at ~12s with rc=3. This is the whole explanation for R11_RC=3 co-occurring with R_LANDED (write landed, pselect never reached timeout, route_done stayed 0, rc=3 had zero discriminative power under mt61 geometry). Now watchdog = wake(3s) + window(20s) + 5 = 28s default, env PSELECT_WATCHDOG_SECONDS overrides. STALL line prints the actual threshold.
(b) STORM-DONE WINDOW CLOSE: after the trigger burst completes, the consumer arms the window timerfd (block_fd) with 1ms -> pselect returns immediately -> route_done=1 -> normal teardown. The window's only job is keeping the fdset kernel copy (poisoned rt_waiter slot) alive until the storm ends; parking the remaining ~16s/round was pure waste. New marker "mt66: window wake armed".
(c) WINDOW LIVENESS GUARDS: (1) consumer waking from ENTER_DELAY after the window closed skips the burst entirely ("mt66: consumer woke AFTER window close" - the exact shape of the 13/18 starved rounds; post-pselect the fdset kernel copy is freed, a late burst = blind writes); (2) mid-burst guard aborts remaining shots if the window closes mid-storm ("mt66: window closed mid-burst"); (3) full-root terminator: root_alive.txt visible -> abort shots (mt64 chmod-000-blinded mt51 for the C stage; root_alive.txt is written by the mt33 child AND-gate ~200ms after CapEff full && euid==0, uid=0 bypasses DAC, works for both R and C stages) ("mt66: root_alive seen").
(d) CANARY PLACEMENT FIX: canary embedding moved BEFORE open_slide_selected_fds (R5/R6 EBADF root cause: planted after the dup pass -> canary fd never opened -> do_select instant EBADF -> 0s window -> 0 shots two cards straight). Field still default-off (PSELECT_NO_CANARY=1 stays in fire()); enable after mt66 baseline is clean.
(e) LATENCY TELEMETRY: all diagnostic lines now carry t=NNNms since window open (mt61 window line, pselect-returned line, every mt19b, every mt25, STALL flags). STALL flags add enter_sched: calls=1+enter=1+no mt19b = starved inside the sched section; calls=1+enter=0 = starved earlier (spin/ENTER_DELAY). Consumer idle loop now usleep(1000) instead of yield spin (with the longer watchdog the spin would pin a little core for 20s+), and exits when route_done. PSELECT_TRIGGER_SHOTS env (default 6, cap 16).

RECIPE (unchanged from mt61, verified reproducible): IonStack exploit Makefile + mt11_v30_src sources + IonStack src/kernelsnitch headers + IonStack src/{su_blob.S,wallpaper_blob.S} + assets/wallpaper.webp; make PROJECT=matisse-OS2.0.6.0.ULKCNXM NDK_ROOT=<ndk-r29>; zero errors, 4 known-harmless unused-variable warnings identical to mt58-mt61 builds.

VERIFICATION: 14/14 marker strings present (mt61 window + mt66 watchdog/window-wake/window-closed/woke-after/root-alive + mt60 STALL flags + mt59 STALL + mt19b + mt25 + enter_sched + t=%lldms x7 + both new env names); ELF aarch64 shared object; DT_NEEDED = libdl.so + libc.so only (bionic-clean); preload.so 187344 bytes (mt61 was 185216, +2128 from mt66); su_daemon byte-identical to mt58/60/61.

FIELD EXPECTATIONS for the first mt66 card: rc=3 becomes meaningful again (only fires after 28s = true inert round); a landed R round with completed storm should finish the whole round in ~30-40s instead of dying at 12s; rounds where the consumer is starved print the new starve markers with exact t= latencies; C1 with R landed keeps all 6 shots (mt64) and now self-terminates on root_alive.txt. Run with: stay-awake-while-charging ON, plugged in, screen on, ideally 15-min load < 20 (the 122106 card ended at 1-min load 215 - MIUI background maintenance; load is recorded, not gated).

NOTE: first 64-hex field in this file is the preload.so hash by convention since v4 (script parses the labeled line).
