# GhostLock (CVE-2026-43499) 漏洞利用操作指南
# Redmi K50 Pro (matisse) - OS2.0.6.0.ULKCNXM

## 一、设备信息总结

| 项目 | 详情 |
|------|------|
| 设备 | Redmi K50 Pro (matisse) |
| 系统 | OS2.0.6.0.ULKCNXM (Android 14) |
| Kernel | 5.10.209-android12-9 |
| 架构 | ARM64 (aarch64) |
| 漏洞状态 | ⚠️ 受影响 (2.6.39 ~ 7.1-rc1 范围内) |
| 必要条件 | CONFIG_FUTEX_PI=y (默认开启) |
| 利用难度 | 本地低权限即可触发，无需特殊能力 |

## 二、你要准备的东西

### 1. 手机上要做的
- ✅ 开启「开发者选项」→「USB 调试」
- ✅ 开启「开发者选项」→「OEM 解锁」
- 用数据线连接电脑，手机弹出提示时点「允许」

### 2. 电脑上要装的工具

你需要安装以下工具（在 Windows 上）：

#### a) ADB 工具
下载 Platform Tools:
https://developer.android.com/tools/releases/platform-tools

解压到比如 `C:\adb`，然后把这个目录加入系统 PATH。

验证安装：
```cmd
adb version
adb devices
```
如果看到你的设备序列号，说明连接成功。

#### b) Python 3 (你已经有)
#### c) ARM64 交叉编译工具链 (用于编译 exploit)

下载 Android NDK:
https://developer.android.com/ndk/downloads

或者用更简单的方法——安装 Termux 在手机上直接编译（推荐新手）：
- 在手机上下载安装 Termux (从 GitHub Release 或 F-Droid)
- 打开 Termux，运行：
```bash
pkg update && pkg upgrade
pkg install clang make git
```

### 3. 获取 Exploit 代码

```bash
git clone https://github.com/CyberMeowfia/IonStack.git
cd IonStack/CVE-2026-43499/exploit
```

## 三、关于「端口」的问题

你不需要开任何端口！整个流程是：

```
你的电脑 --(USB数据线/ADB)--> 手机
```

所有操作都是通过 USB 线在本地进行的。不需要：
- ❌ 不需要开防火墙端口
- ❌ 不需要网络服务器
- ❌ 不需要 WiFi 调试
- ✅ 只需要一根 USB 数据线 + ADB

### ADB 的工作方式：
```
电脑 (adb push/pull/shell) <--USB线--> 手机 (adbd 守护进程)
```

ADB 默认使用的是本机回环，不对外开放端口。

## 四、操作步骤

### 步骤 1：确认 ADB 连接

```cmd
adb devices
```
应该显示：
```
List of devices attached
XXXXXXXX    device
```

### 步骤 2：获取内核符号（关键！）

Exploit 需要知道内核函数的地址。在手机上运行：

```cmd
adb shell "cat /proc/kallsyms | head -20"
```

这会显示内核符号表。你需要找的关键符号：
- `_text` — 内核基址
- `prctl` 或 `SyS_prctl` 或 `__arm64_sys_prctl`
- `core_pattern` 或 `core_pattern_setup`
- `inet6_protos`

把输出保存下来：
```cmd
adb shell "cat /proc/kallsyms" > kallsyms.txt
```

### 步骤 3：编译 Exploit

#### 方法 A：在手机上编译（推荐新手）

1. 把 exploit 代码推送到手机：
```cmd
adb push CVE-2026-43499/exploit /data/local/tmp/exploit_src/
```

2. 在手机上用 Termux 编译：
```bash
cd /data/local/tmp/exploit_src/
clang -o exploit exploit.c -lpthread -static
```

#### 方法 B：在电脑上交叉编译

```cmd
REM 设置 NDK 路径
set NDK=C:\android-ndk
set CC=%NDK%\toolchains\llvm\prebuilt\windows-x86_64\bin\aarch64-linux-android31-clang

%CC% -o exploit exploit.c -lpthread -static
```

### 步骤 4：运行 Exploit

```cmd
adb push exploit /data/local/tmp/
adb shell "chmod 755 /data/local/tmp/exploit"
adb shell "/data/local/tmp/exploit"
```

如果成功，你会看到：
```
# id
uid=0(root) gid=0(root)
```

## 五、已提取的文件

以下文件已保存在 extracted 目录中：

| 文件 | 说明 |
|------|------|
| `boot_final.img` | 完整 64MB boot 镜像 |
| `kernel.Image` | 解压后的 44.7MB kernel (PE/EFI 格式) |
| `kernel_efi_partial.bin` | 部分解压的 kernel 数据 |

## 六、常见问题

### Q: ADB 显示 unauthorized？
A: 手机上会弹出「允许 USB 调试」对话框，点允许。如果没弹出，执行：
```cmd
adb kill-server
adb start-server
```
然后重新插拔 USB 线。

### Q: 需要 root 才能运行 exploit 吗？
A: 不需要！GhostLock 的 exploit 就是从普通用户提权到 root 的。

### Q: 手机会变砖吗？
A: Exploit 失败通常只会导致手机重启或卡顿，不会永久损坏。但操作 boot 分区时要小心。

### Q: /proc/kallsyms 显示全是 0？
A: 有些 ROM 限制了 kallsyms 的读取。替代方案：
```cmd
adb shell "su -c 'cat /proc/kallsyms'"
```
或者从 kernel Image 中静态提取符号（需要更复杂的分析）。

## 七、注意事项

⚠️ 此 exploit 仅用于个人设备的安全研究。
⚠️ 不要在主力机上测试，建议先备份数据。
⚠️ Google 已为此漏洞支付 $92,337 赏金，说明其严重性。